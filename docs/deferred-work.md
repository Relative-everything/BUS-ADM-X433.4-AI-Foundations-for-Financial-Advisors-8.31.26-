# Deferred work — the single register

**THIS FILE IS THE ONLY PLACE OPEN WORK IS TRACKED.** Before this file existed,
every phase wrote its own `§N.8 What Phase N leaves open` section into
`docs/repo-updates-plan.md`, and three of them accumulated in parallel with no
way to tell which entries were still true. §13.8, §15.4 and §16.8 are folded in
below and are now **history of what each phase found**, not a to-do list. A
later phase adds rows here; it does not start a fourth list.

Created 2026-08-25 in Phase 3.6, consolidating `docs/repo-updates-plan.md`
§13.8 (Phase 2, eight items), §15.4 (Phase 3, seven items) and §16.8
(Phase 3.5, ten items), plus `MAINTAINING.md`'s "Known follow-ups" and what
Phase 3.6 itself deferred.

## Where things stand

**Read this first if you have been away.** Written 2026-09-03 by
`audit/AUDIT-2026-09-03.md`, which re-measured every open row against `main` at
`8442160` (the merge of PR #18) and replaces the header of 2026-08-27. Branch
`claude/deferred-work-audit-reprioritize-kh0djg`.

**The calendar moved.** Session 1 was taught on 2026-08-31 and its page stays
live. Session 2 is taught on 2026-09-14. Sessions 3 and 4 follow on dates
`README.md` still marks unverified. Proximity to teaching is this register's
tie-break, so a row on `session-2` now outranks an equal row anywhere else, and a
`session-1` row is a live-site defect rather than a pre-class one.

**The gate exits 0** on every in-repo check. `verify-style.mjs` exits 1 only for
its hard-coded skill path, and the Wolfram verifier has no input in the tree;
both are DW-080.

**Three decisions are closed, do not re-open them:** `session-0.1` stays, stays
linked, and is permanently excluded from the ungraded-repo purge (DW-057); DW-048
is accepted as-is; Session 5 is a student presentation meeting, so no `session-5`
lesson file is owed, now or later.

**What the audit changed in this file.** One row had closed on the tree without
being amended (DW-009). Seven rows keep their substance with corrected figures,
locators or severity (DW-005, DW-022, DW-024, DW-043, DW-055, DW-056; DW-064 is
raised to MAJOR after the defect was reproduced in a browser). Nineteen rows'
line numbers had drifted; the quoted string is the locator. Sixteen rows are new
(DW-071 to DW-086). Every change is a dated note beside the original text.

**What is broken**, meaning a student sees it on a served page: DW-064 (typing a
capital U into any text box reveals every withheld panel, all five lessons);
DW-001/002/003 (the false retrieval score, three times in `session-3`); DW-071
(a hub sentence its own annotation calls wrong); DW-072 (a constructed chart
read as a measurement in `session-1`); DW-012, DW-004, DW-013, DW-008 (figures
in `session-4` and `session-3`); DW-077 (the Case facts control off-screen at
380px on `session-0.1` and `session-1`); and DW-041 (the case denies the
engagement four lessons assume).

**Do these next.** A live defect on the next-taught page outranks a
contradiction, which outranks a pending decision, which outranks a mechanical
fix; proximity to teaching breaks ties; shared constructs land together.

*Mechanical, no decision needed, before 2026-09-14:*

1. **DW-064 + DW-085** — guard the eight Shift+U handlers against text inputs and
   give the override one label in five files; add the reproduction to
   `verify-browser.mjs`.
2. **DW-071** — narrow the hub sentence as its own annotation says.
3. **DW-066 + DW-074** — propagate `COLD_CHECKS` and the reader-addressed pacing
   block from `session-1` to the four other lessons, one commit.
4. **DW-007, DW-008, DW-042, DW-052, DW-049, DW-032, DW-033** — seven
   minutes-each edits, one commit.
5. **DW-078 + DW-080** — README's grading sentence and term label; the
   verification surface's paths.

*Decisions, ordered by what each unblocks:*

1. **DW-041** — record the engagement in §A.5 or rewrite what four lessons
   assume. The only BLOCKING row on the next-taught page.
2. **DW-003** — lift the VALOPT constraint; unblocks DW-001, DW-002 and DW-076,
   one commit.
3. **DW-050** — standalone or spiral; settles DW-062 and DW-075 and how DW-066
   propagates.
4. **DW-056** — the verify gates, the two in `session-2` first.
5. **DW-072** — label the §02 chart illustrative, or replace it.
6. **DW-021 and DW-073** — the `session-2` items first: the PTCF source's chip
   and the `:1576` source.
7. **DW-082** — are Phases 5 to 7 still the plan?
8. **DW-044 to DW-047, DW-058, DW-059** — one ratification closes six rows.
9. **DW-084** — the two contested rulings.

The full ranking, the rubric behind it, and where judgement overrode the score
are in `audit/AUDIT-2026-09-03.md` §5 to §7.

---

## How to read a row

| Column | Means |
|---|---|
| `id` | Stable. Never renumbered, never reused. A closed row keeps its id. |
| `phase` | The phase that **raised** it, not the phase that will fix it. |
| `file:line` | Where it lives, when it lives somewhere. Line numbers drift; the quoted string in the description is the durable locator. |
| `severity` | **BLOCKING** — ~~a student is marked against something wrong, or~~ a page states a fact the repo's own source contradicts. *(Clause 1 struck 2026-08-27: nothing in this repository is graded and nothing ever will be, so no student can be marked against anything here. Clause 2 stands unchanged, which is why DW-003 and DW-041 are still BLOCKING — both are contradictions, not grading defects.)* **MAJOR** — a claim or figure that a reader could repeat and be wrong. **MINOR** — real, bounded, and nothing else leans on it. **HOUSEKEEPING** — no reader is affected. |
| `owner` | **instructor** — needs a decision, a reading of a source, or a curriculum call. **Claude Code** — mechanical once the decision exists. |
| `status` | **open** · **open-upstream** (the fix is not in this repository) · **superseded** (the row's premise stopped being true; kept so it is not re-raised) |

**Owner is not seniority, it is what the work needs.** A row owned by the
instructor is blocked on a judgment nobody else can make. A row owned by Claude
Code is blocked on nothing.

---

## Open

| id | phase | description | file:line | severity | owner | status |
|---|---|---|---|---|---|---|
| DW-001 | 3.6 | `session-3` §03's preset-1 verdict says the 2023 appraisal *"scores 0.0: it shares no scored term with the question it is the answer to"*. It scores **3.318005**, ranks **third of ten**, and appears in the top-4 display. It shares the term *value* with the query. Measured by executing the page's own retriever.  **Audited 2026-09-03:** still present, now at `session-3:2030`. Land with DW-002, DW-003 and DW-076 in one commit. | `session-3/index.html:2058` | MAJOR | Claude Code | open |
| DW-002 | 3.6 | The same false figure in the appendix C4 complication: *"the appraisal — scored zero and never appeared"*.  **Audited 2026-09-03:** still present, now at `session-3:2590`, inside the panel headed *"Complication — instructor introduces, nobody re-speaks"* (DW-076). | `session-3/index.html:2618` | MAJOR | Claude Code | open |
| DW-003 | 3.6 | The same false figure **inside a scored answer key**: VALOPT option 2 says *"Asked about Meg's CPC shares, it scored 0.0, because it shares no term with the question."* VALOPT is out of scope for Phase 3.6 by explicit constraint, so DW-001 and DW-002 were left alone too rather than putting a contradiction inside one lesson. **Close all three together.** The stronger true version is available and unused: of the four scored query terms, `meg` and `cpc` have df=0 and appear in NO chunk at all.  **Audited 2026-09-03:** still present, now at `session-3:2187`. The edit is minutes once the VALOPT constraint is lifted; ranked the second decision after DW-041. | `session-3/index.html:2215` | **BLOCKING** | instructor | open |
| DW-004 | 3.6 | `session-4`'s compound evidence reasoner offers *Watermark found · No watermark found · Detector errored or unsupported*. The newly retrieved `src-synthid-text` states the detector's own output is three-way — **watermarked, not watermarked, or uncertain**, against two tunable thresholds. A tool error is not the detector's third verdict. The six-cell matrix is a scored key, so this changes what the exercise reveals.  **Audited 2026-09-03:** `aOpts` unchanged, now at `session-4:2980`. | `session-4/index.html:3012`, matrix at `:3027` | MAJOR | instructor | open |
| DW-005 | 3.6 | Ten of `src-synthid`'s eleven references remain unsourced after `src-synthid-text` closed one. C2PA claims (`:1540`, `:1544`); the tournament mechanism, whose real authority is the Nature paper nobody has loaded (`:1553`, `:1568`); image, video and audio robustness, which a text page cannot carry (`:1571`, `:1585`); the adoption paragraph, which carries two chips on one line (`:1588`); the formatter claim (`:1621`); and a sentence sourced to Zhao and three others (`:1628`). Enumerated by line in the `src-synthid` record so the generated queue carries them.  **Audited 2026-09-03:** `session-4` now carries 11 `src-synthid` chips and 2 `src-synthid-text`; the *ten of eleven* count is stale, the substance stands. | `session-4/index.html`, ten lines | MAJOR | instructor | open |
| DW-006 | 3.6 | `session-4:1588` asserts what *"Google's own current page states"* — a page no build has ever loaded. Marked `[UNCONFIRMED]` with a CLAIM annotation in Phase 3.6; the resolution is to read the page and date the reading, or narrow the sentence to the page that was read.  **Audited 2026-09-03:** still present, now at `session-4:1563`, and the span directly after `[UNCONFIRMED]` is an **H** chip, so an H confidence sits on an unconfirmed claim; that chip is this row's second defect. | `session-4/index.html:1588` | MAJOR | instructor | open |
| DW-007 | 3.6 | `session-1` §05's price citation carries `data-src="src-aa"` on a sentence whose source is `src-pricing`. Noticed while re-dating that citation; the chip is a Phase 3 rewiring decision and was not touched.  **Audited 2026-09-03:** now at `session-1:1865`; both chips on the line are `data-src="src-aa"`, the second directly after *"Prices: Anthropic Claude Platform Docs, 25 August 2026"*. Rewiring a chip to the source the sentence names is mechanical, so the owner is Claude Code. | `session-1/index.html:1883` | MINOR | Claude Code | open |
| DW-008 | 3.6 | `session-3`'s retrieval margin flags at `marg<12` while the sentence it prints says *"less than an eighth of the winning score"*. An eighth is 12.5%. A margin of 12.3% is less than an eighth and is not flagged.  **Audited 2026-09-03:** `marg<12` still at `session-3:2057`. | `session-3/index.html:2085` | MINOR | Claude Code | open |
| DW-009 | 3.6 | `index.html`'s footer says *"UC Berkeley Extension, Fall 2026"*. It is a term label on the offering rather than a deadline, so Phase 3.6's deadline conversion left it. Removing it makes the site's vintage unstated while its 2026 tax parameters, AFRs and exclusion amounts stay 2026-specific. A decision about what the hub claims to be.  **Audited 2026-09-03:** **closed on the tree.** `grep -c "Fall 2026" index.html` returns 0; the string left in `a5ede33` (Pass 3, 2026-08-29) and the footer now reads *"BUS ADM X433.4 — AI Foundations for Financial Advisors · UC Berkeley Extension · Instructor: Jared Winkers"*. The decision this row asked for was taken by the title pass. Residue: `README.md:3` still says *"Fall 2026"*, carried as DW-078. | `index.html:1108` | MINOR | instructor | closed |
| DW-010 | 3.6 | The repository has **no `CLAUDE.md`**. Phase 3.6's brief listed one as required reading and there is none at the root or under `.claude/`. Either the repository conventions live entirely in `MAINTAINING.md` and `EDITORIAL.md` and that is fine, or a `CLAUDE.md` is owed. | — | HOUSEKEEPING | instructor | open |
| DW-012 | 3.5 | `session-4` §08's audit exhibit cites *"Buy-Sell Agreement, **Cole Precision Components Inc.**, section 4.2"*. A 2014 instrument cannot carry a name the company did not take until 15 August 2016. The section says two of its six items are designed defects, so this is either a third with no label or drift with a pinpoint locator on it.  **Audited 2026-09-03:** still present at `session-4:1779-1780`, line-wrapped across *"Cole Precision Components Inc.,"* / *"section 4.2, pages 7-8."*. | `session-4/index.html:1804` | MAJOR | instructor | open |
| DW-013 | 3.5 | `session-3` teaches 3.3% as the grounded hallucination floor and labels it *"Best model, grounded"*. Every per-model rate is still on the live board, but 3.3% now ranks third and the floor is 1.8%. The measurements hold; the superlative is stale.  **Audited 2026-09-03:** `HALL` array at `session-3:2113-2118` still labels 3.3 *"Best model, grounded"*; the CLAIM and `[UNCONFIRMED]` sit at `:2249-2250`. | `session-3/index.html:2146`, `:2277` | MAJOR | instructor | open |
| DW-014 | 3.5 | `EDITORIAL.md` Part A's population is `ALL_LESSONS`, which excludes `index.html`. A16 therefore polices the form of every marker in the five lessons and none on the hub, while the generated register lists all of them. Widening the population is a decision about what `EDITORIAL.md` governs. | `EDITORIAL.md:124`, the `authoredProse` population | MINOR | instructor | open |
| DW-017 | 3.5 | `index.html:1013` says *"Every tax and legal characterisation attached to this case is posed as a question, not a holding"*, which the injected extract contradicts twelve lines below on the same screen. Instructor-facing and erring toward more verification, so it was flagged rather than changed.  **Audited 2026-09-03:** now `index.html:989-990`. | `index.html:1013` | MINOR | instructor | open |
| DW-018 | 3.5 | `src-zhao`'s `last_retrieved` is unresolved. The authors' repository returned the official BibTeX and six indexes corroborate it, but arxiv.org, the NeurIPS proceedings, OpenReview, the ACM DL and Semantic Scholar are all blocked from this build environment. A repository is not the paper and a search index is not a retrieval. | `SOURCES.md`, `src-zhao` | MINOR | instructor | open |
| DW-019 | 3.5 | `src-owasp`'s citation points at what OWASP itself describes as a historical archive. The teaching claim survives — prompt injection is LLM01:2026 in the new edition, published 2026-08-04 — and the `link` field does not. | `SOURCES.md`, `src-owasp` | MAJOR | instructor | open |
| DW-020 | 3 | Eight chips should not be confidence chips at all: a figure-label legend, two of the page's own methodological caveats, a declared assumption, and the chip component reused as a category badge. Changing them moves A15, V4 and `verify-migration` check 18 at once. | `docs/chip-rewiring.md` §4 | MINOR | instructor | open |
| DW-021 | 3 | Five sources are listed by a lesson that never cites them and none is exempt by kind: `src-google-ptcf` and `src-pricing` in `session-2`, `src-finra2409` and `src-secpri` in `session-3`, `src-finra2409` in `session-4`. `src-google-ptcf` is the sharpest — `session-2` §03 teaches Persona-Task-Context-Format and cites its source nowhere. Reported by A15 on every run, at ADVISE.  **Audited 2026-09-03:** A15 still reports all five; `session-2` §03 still has no `src-google-ptcf` chip and is taught 2026-09-14, so its two keys go first. | four footers | MAJOR | instructor | open |
| DW-022 | 3 | `src-aa`'s versioning incoherence, and the livebench-versus-Artificial-Analysis attribution. A later version string carries data identical to an unversioned one and an earlier version string carries different data, so the version is not tracking the data. `grep -rniI livebench` returns 0 repo-wide. Surfaced with evidence both ways at §5.6 and deliberately not resolved.  **Audited 2026-09-03:** premise partly changed. `SOURCES.md` `src-aa` now carries `index_version: divergent, see the three pulls below` and a `retrieval_note` naming the partial-date artifact; `DATA-PULL.md` reports the three precondition failures. The incoherence is documented rather than hidden; the version behind 12 references is still unresolved and needs a fresh full-dated pull. | `docs/repo-updates-plan.md` §5.6 | MAJOR | instructor | open |
| DW-023 | 3 | A15's severity is still ADVISE. Its precondition, `data-nochip`, is met, so promoting it to HARD is now a decision rather than a blocked one. | `scripts/editorial-baseline.json` | MINOR | instructor | open |
| DW-024 | 3 | **55 of 59 records carry an empty `last_verified`**, 3 more are *not applicable* and exactly **one** is populated. That is the measurement, not a backlog: `last_verified` may only be written by a human at a terminal, and no phase may close this row on its own. `docs/source-verification-queue.md` carries the live counts and the queue ordered by reference count.  **Audited 2026-09-03:** the queue now reads **60** records, **56** EMPTY, 1 populated, 3 not applicable; 144 of 177 references stand behind an EMPTY `last_verified`. | `SOURCES.md` | MAJOR | instructor | open |
| DW-025 | 3 | Most records carry at least one `[UNVERIFIED, needs source]` field — usually a missing retrieval date or publisher on a source whose identity is not in doubt. `BIBLIOGRAPHY.md` prints every one.  **Audited 2026-09-03:** `verify-sources` reports 53 records with at least one such field. | `SOURCES.md` | MINOR | instructor | open |
| DW-026 | 2 | **F5** — two new consecutive-component adjacencies, `session-2` and `session-4`. Re-anchor or re-tag. Curriculum. | — | MINOR | instructor | open |
| DW-027 | 2 | **F6** — `session-2`'s print nondeterminism. Pre-existing and by design; fixing it changes interaction logic. | `session-2/index.html` | MINOR | instructor | open |
| DW-028 | 2 | **A21** — D21 is mechanically checkable and the check is not built. Population is 12 items; §13.2 A is the seed data. A rule firing on the wrong twelve is worse than no rule, which is why it is not built yet. | `scripts/verify-editorial.mjs` | MINOR | Claude Code | open |
| DW-029 | 2 | `validate_lesson.py` **V2** matches any `href`, including a plain `<a href>`, which makes no request. Every footer citation hyperlink is reported as an external request, so a lesson cannot both link its sources and pass V2. **The fix belongs in the skill, not this repository**, and the skill must not be edited from here.  **Audited 2026-09-03:** the synced skill's `validate_lesson.py:61` regex is unchanged. | upstream skill | MAJOR | instructor | open-upstream |
| DW-030 | 2 | `validate_lesson.py` **C2** emits an INFO em-dash count over the raw file, so it includes CSS, comments, attributes and the injected span, and misses `—` and `&#8212;` entirely. Its *"policy unratified"* parenthetical is wrong now that D1 ratifies it. Must be corrected or retired upstream before A8 and A9 go hard.  **Audited 2026-09-03:** the synced skill's `validate_lesson.py:189-194` is unchanged. | upstream skill | MINOR | instructor | open-upstream |
| DW-031 | — | Self-host the three font families, so a lesson renders with no network request at all. The live console's runtime request is separate and stays: it fires only on a key the reader supplies. | all lessons | MINOR | Claude Code | open |
| DW-032 | — | `docs/spine-brief.md`'s opening paragraph reads *"Nothing here is implemented"*. The spine has been implemented since `93904d7` and lives in a string literal in `scripts/inject-case.mjs`. D17. | `docs/spine-brief.md:3` | HOUSEKEEPING | Claude Code | open |
| DW-033 | 3.6 | `docs/repo-updates-plan.md` §14 says *"Three stay open and untouched"* and then names four: the adjacencies, the print nondeterminism, A21 and `validate_lesson` V2. Four rows are carried here (DW-026 through DW-029) because four were named. | `docs/repo-updates-plan.md:2800` | HOUSEKEEPING | Claude Code | open |
| DW-041 | 3.6 | **The §07 meeting excerpt presupposes an advisory engagement §A.5 denies.** The excerpt has an Advisor speaking; §A.5 records **no investment adviser of record**, and every lesson assumes an engagement between the Coles and the student's firm. Phase 3.6 moved the excerpt into CASE.md Part O, which does not settle the conflict but puts both statements in one file with the conflict written next to the excerpt. Resolving it means amending §A.5 to record the engagement or rewriting what four lessons assume.  **Audited 2026-09-03:** unchanged: `CASE.md:89` (§A.5, *"none — assets held direct at a custodian"*) against Part O `:1127-1136`. Ranked the first decision, because it is the only BLOCKING row touching the page taught 2026-09-14. | `CASE.md` Part O, the meeting excerpt | **BLOCKING** | instructor | open |
| DW-042 | 3.6 | `session-3` §07's rubric element 5 matches `two years` in its regex, left from when the lesson called the 2023 appraisal two years old. The excerpt and §F.7 both say three. Harmless as an alternation, wrong as a record.  **Audited 2026-09-03:** regex still carries `two years`, now at `session-3:2357`. | `session-3/index.html:2385` | MINOR | Claude Code | open |
| DW-043 | 3.6 | The `.case-*` layout CSS is still six hand-written copies, one per lesson, and nothing asserts they agree. The viewer's own rules are emitted into the injected span instead, so the two halves of one component are now maintained differently. Either sweep the layout rules into the span too, or add the six-way md5 pairing the console blocks already have.  **Audited 2026-09-03:** the six copies of the 20 `.case-*` rules **agree today** (md5 `563230ac` in all six files, measured on the rule text after `/* STYLE:END */`); what is missing is the check, not agreement. | six lessons, after `/* STYLE:END */` | MINOR | Claude Code | open |
| DW-044 | 4 | **Ratchet lowered, recorded under the `MAINTAINING.md` ratchet rule.** Phase 4 moved every instructor note out of the pages into `instructor-notes/`, and each em dash inside a note went with the prose that carried it. `A9.files` falls in all five lessons: session-0.1 entity **7 -> 3**, session-1 entity **81 -> 76**, session-2 entity **66 -> 65**, session-3 entity **13 -> 12** and literal **92 -> 91**, session-4 entity **84 -> 83**. No dash was added or converted; `test-editorial-regions` T7 requires the baseline to reproduce the classifier exactly, so the lowering is not deferrable to a later commit. `A9b` (R11, 6 blocks / 6 dashes) did not move: the standing-ritual note sits beside the `.ritual` block, not inside it, so it was R1 and not byte-shared boilerplate. Recorded so the coverage the ratchets lost is visible rather than silent.  **Audited 2026-09-03:** record row; the baseline now reads 0.1 3, s1 67, s2 66, s3 98, s4 83 and T7 reproduces it. Closes on one ratification with DW-045 to DW-047, DW-058 and DW-059. | `scripts/editorial-baseline.json`, `A9` | HOUSEKEEPING | instructor | open |
| DW-045 | 3.6 | **Three ratchets lowered before the rule existed, recorded retroactively.** `MAINTAINING.md` now requires every lowered `scripts/editorial-baseline.json` figure to open a row in the same commit; these three predate it and had none. `A9` session-1 entity **83 -> 81**, 2026-08-25 — Phase 3.6 collapsed §05's two Sonnet 5 price rows into one after the vendor cancelled the scheduled rise, and the two dashes carrying the *through-today* and *from-1-Sep* qualifiers went with the rows. `A9` session-3 literal **94 -> 93**, same day — the handoff deadline stopped being *the end of Session 4, six days out* and became the relative 48-hour form, and the dash before *six days out* went with it. `A9` session-3 literal **93 -> 92**, same day — the §07 meeting excerpt moved into `CASE.md` Part O, so its dash is now inside the injected span, which `authoredProse` masks out. **That third one deleted no dash at all: it changed region**, and the excerpt renders character for character as it did, which is the case the rule most needs on the record because it is the one a reader would otherwise mistake for prose loss. All three are already narrated in the `A9` note of the baseline file itself; this row is the index into it, in the register a reader checks. Nothing to fix — read the three, confirm each was sanctioned, and close.  **Audited 2026-09-03:** record row; closes on the same ratification as DW-044. | `scripts/editorial-baseline.json`, `A9` | HOUSEKEEPING | instructor | open |
| DW-046 | 4 | **Ratchet lowered, recorded under the `MAINTAINING.md` ratchet rule.** `A9` session-4 entity **83 -> 82**. The fifteenth instructor note was found after DW-044's commit, by two independent adversarial sweeps that both landed on the same block: `session-4`'s *"The complication, for the instructor only"* panel in `#sD`. It was excluded on the first pass with the three other Complication blocks, and that exclusion was wrong on its own stated grounds — the other three state a substantive fact to the room, this one is nothing but delivery direction (*"Introduce at minute 11..."*, *"Ask both sides to re-price on that basis"*), it is the only one of the four carrying an explicit instructor marking, and removing it costs no curriculum: the phase is already defined for students in the table above it and the re-vote flow references that table, not the panel. Its one em dash went with it. Same cause as DW-044, separate row because the rule counts lowering events and this one landed in its own commit.  **Audited 2026-09-03:** record row; closes on the same ratification as DW-044. | `scripts/editorial-baseline.json`, `A9` | HOUSEKEEPING | instructor | open |
| DW-047 | 5 | **Ratchet lowered, recorded under the `MAINTAINING.md` ratchet rule.** The ungraded-repo purge removed every assertion of a grade, a weight, a submission or a between-session deadline from `index.html` and sessions 1-4, and the em dashes inside those sentences went with them. `A9` session-2 entity **65 -> 64** (the dash in *"the final project is 50% of the course grade - so this single deliverable is 35%"*); `A9` session-3 literal **91 -> 86** (one in the *"Due before Session 5 - revised"* card label, two in the assignment-terms source note, two in the citation panel's *"the assignment requirement - three or more sources, at least one scholarly -"*). `index.html`, session-1 and session-4 did not move: nothing deleted from them carried a dash inside `authoredProse`. `A9b` (R11) held at 6 blocks / 6 dashes after the reverted edit in DW-048. No dash was added or converted.  **Audited 2026-09-03:** record row; closes on the same ratification as DW-044. | `scripts/editorial-baseline.json`, `A9` | HOUSEKEEPING | instructor | open |
| DW-049 | 5 | **`MAINTAINING.md`'s console-pairing command reports a false mismatch.** The documented loop greps `'LM:BEGIN'`, which is a substring of `LMBOX:BEGIN` and `LMSTYLE:BEGIN`, so the `LM:BEGIN` pass starts at the first of the three fences and its two md5s never match. Measured at HEAD `2c71767`, before any edit this turn: `bbb3b955` against `335b3f3b`. The fences themselves are fine - ranged properly on `LM:BEGIN v1` to `LM:END v1` both files give `b1139a6e`. A pairing check that always fails is a pairing check nobody reads.  **Audited 2026-09-03:** reproduced at `8442160`: the documented loop gives `bbb3b955` against `a0172c9b` on the `LM:BEGIN` pass; ranged on `LM:BEGIN v1`..`LM:END v1` both files give `b1139a6e`. | `MAINTAINING.md`, the pre-push console-pairing loop | MINOR | Claude Code | open |
| DW-050 | 5 | **Must the sessions be fully standalone? The Tier D constructs are held open on this question.** The ungraded-repo purge deleted every between-session *obligation* and left every between-session *pedagogical* reference standing, by explicit instruction. Those are ratified constructs and there are three kinds: the retrieval-bridge and callback openers (`session-2:1249` *"Fourteen days have passed since Session 1"*, `session-3:1219-1220` the Session 2 citation-order recall, `session-4:1339` *"In Session 1 you redacted the Cole facts on instinct"*); the case spine's escalating-artifact chain (`session-4:1778` and `:1781-1795`, which take the Session 2 IDGT prompt and the Session 3 buy-sell extraction as test material and ship fallback copies for anyone who missed either); and the spiral declarations (`session-3:1452`, `session-4:1670`, `session-1:2132`). **The tension is real and nobody has settled it.** A repo that is a live visual aid followed during one lecture argues for each file standing alone; a spiral curriculum argues that the callbacks ARE the pedagogy and a session taught without them teaches less. `session-4:1781` already hedges - it ships fallback artifacts for the two sessions it depends on, which is the standalone answer applied to exactly one construct and nowhere else. Decide once, then apply it to all three kinds or to none. Nothing here is a defect until that decision exists.  **Audited 2026-09-03:** `session-2:1226` *"Fourteen days have passed since Session 1"* is true for 2026-09-14. Deciding this settles DW-062 and DW-075 and how DW-066 propagates. | five lessons, enumerated above | MINOR | instructor | open |
| DW-051 | 4 | **`instructor note` survives in six files and the fix is upstream in the skill.** `index.html:19` and each lesson's `:19` (`:24` in `session-0.1`) carry `--warn rust   = caution, failure states, instructor notes` inside the `STYLE:BEGIN`/`STYLE:END` fence. The fence is a byte-exact copy of `/root/.claude/skills/synced/interactive-lesson-builder/assets/tokens.css`, written by `restyle_sweep.py`; the string is at `tokens.css:6` and again at `references/design-system.md:26`. The repo's classifier calls all six **R4**, which `EDITORIAL.md` marks *never in scope, owned by `verify-style.mjs`* - so the standing purge rule is not violated by them, and `MAINTAINING.md` now says so next to the entry. Hand-editing them makes all six files STALE and `verify-style.mjs` exits 1; the next sweep restores the string. **The fix belongs in the skill and the skill must not be edited from this repository** (the DW-029 precedent). Two lines to change upstream, then re-run the sweep.  **Audited 2026-09-03:** the synced skill's `tokens.css:6` is unchanged. | upstream skill, `assets/tokens.css:6` and `references/design-system.md:26` | MINOR | instructor | open-upstream |
| DW-052 | 4 | **`session-3:2424` carries an inert `split:true`.** Phase 4 removed the instructor aside from the quiz feedback string at `:2463`, which was the flag's only reader. The datum still marks *the item designed to split the room* and nothing consumes it. Harmless — the quiz renders identically and the page throws no error — but a data field with no consumer is the shape a later reader mistakes for a bug. Either delete the key or give it a consumer; deleting it is a data edit, which is why Phase 4 flagged it instead.  **Audited 2026-09-03:** `split:true` now at `session-3:2401`. | `session-3/index.html:2424` | MINOR | Claude Code | open |
| DW-053 | 4 | **`A8`'s `session-3` minority carries three units of unearned slack.** The baseline records `minority: 15`; the rule's declared population is `authoredProse` and the checker computes `min(literal, entity)` over exactly that, which is **12** today and was **13** before Phase 4. `session_3_reconciliation` in the same file explains 15 as the whole-file entity count and says both figures are correct — but `A8` does not read the whole file, so 15 is the wrong population for this rule and the ratchet has been loose since it was written. Lowering it is a one-line edit; deciding whether the reconciliation note survives that edit is not. | `scripts/editorial-baseline.json`, `A8.files.session-3` | MINOR | instructor | open |
| DW-054 | 4 | **Three instructor-addressed constructs carry no marker, so the Phase 4 definition could not see them.** That definition required one of four markings; these have none. (a) `session-0.1:1750` *"Say that out loud before the room leaves. It is the spiral, and it is what stops each of these sessions reading as a standalone tour."* — only the teacher speaks before the room leaves, and it is a bare `<p>`. (b) **The `.verify` gate family, 13 occurrences across all six files**, every one addressed to the person teaching (*"Verify before teaching any of it as settled"*) and every one also a disclosure the student is owed — see DW-056. (c) The instructor-operated control captions, `session-2:1930` *"Instructor: room percentage voting"* and `session-0.1:1708` *"Room tally, instructor keys in the poll counts"*, which label inputs only the person running the room can fill; removing them leaves an unlabelled control. All three kinds were left in place. The question is whether the marker set should widen or whether these are correctly lesson content.  **Audited 2026-09-03:** sites now `session-0.1:1681`, `:1723`; `session-2:1907`. | `session-0.1:1750`, `:1708`; `session-2:1930`; the 13 `.verify` gates | MINOR | instructor | open |
| DW-055 | 5 | **One lesson forbids what three others permit, on the final project's data.** `session-1:1960` says in bold *"Not a de-identified client — a constructed household with invented figures that behave like real ones."* `session-2:1258`, `session-3:1231` and `session-4:1245` each say students build Part 1 *"using your own synthetic or de-identified data."* `session-2:1737` goes further: *"Synthetic or fully de-identified facts only."* Measured this turn by grepping `de-identif` across all six lessons and `CASE.md`. Three lessons treat de-identified client data as acceptable and one treats it as the error the section exists to teach against. This is a curriculum call, not an edit: either de-identified data is permitted for Part 1 or it is not, and whichever it is, four lessons say it.  **Audited 2026-09-03:** **premise changed, severity lowered to MINOR.** The bold sentence at `session-1:1960` was deleted by Pass 2 (`acd691f`; DW-058 carries its dash). What survives in `session-1` is the CFP-mapping cell at `:2113`, *"de-identification as a standard the name alone does not meet"*, which is consistent with `session-2:1714` *"Synthetic or fully de-identified facts only"*. The four-lesson contradiction is gone; a one-sentence definition of *de-identified* for Part 1 is still owed once. | `session-1/index.html:1960` against `session-2:1258`, `session-3:1231`, `session-4:1245` | MINOR | instructor | open |
| DW-056 | 5 | **Thirteen `.verify` gates guard tax, legal and regulatory characterisations, and none of them has been closed.** Inventoried this turn: `index.html:1012`; `session-0.1:1037`, `:1659`; `session-1:1321`; `session-2:1211`, `:1615`; `session-3:1179`, `:1595`, `:1688`, `:2487`; `session-4:1198`, `:1369`, `:1764`. **The split is six and seven, not seven and six — the earlier statement of this row inverted it and then enumerated seven under a heading that said six.** Recounted by classifying each site on whether it carries the shared sentence *"Every tax and legal characterisation attached to this case is posed as a question, not a holding"*: **6 are that shared case gate** (`index.html:1012`, `session-0.1:1037`, `session-1:1321`, `session-2:1211`, `session-3:1179`, `session-4:1198` — one per file, §7872 against §1274 on a demand note issued for property, the discount, and the two Illinois rates). **7 are section-specific**, each a different body of law: `session-0.1:1659` recordkeeping and supervision; `session-2:1615` IRC §671 and the grantor-trust rules; `session-3:1595` state recording-consent law, where the gate's own text says published compilations disagree and tells the instructor not to resolve it at the podium; `session-3:1688`/`:2487` a planning rationale inside a scored answer panel; `session-4:1369` Regulation S-P §248.3 applied to synthetic facts; `session-4:1764` Regulation S-P as amended 2024 and its two compliance dates. **`session-1` carries exactly one, `session-1:1321`, and it is the shared case gate** — the only one of the 13 in the file taught next, which is why this row outranks the other pending decisions. **Every one of the 13 is a tax, legal or regulatory characterisation** — that is the whole population, with no exceptions. Count only; nothing here asserts any of them is wrong. Closing a gate needs a human reading primary authority, which is `last_verified`'s rule (DW-024) applied to prose instead of to sources.  **Audited 2026-09-03:** by `class="verify"` there are **10** gates, not 13: `index.html:989`; `session-0.1:1014`, `:1636`; `session-1:1308`; `session-2:1188`, `:1591`; `session-3:1156`, `:1571`; `session-4:1175`, `:1740`. Three of the thirteen sites above are inline *"Verify…"* sentences rather than gates (`session-3:1601`; the shared footer note at `session-3:1820` / `session-4:2021`; `session-4:1616`). None is closed. `session-2` carries the shared case gate and the IRC §671 gate, both on the page taught 2026-09-14, so those two go first. | 13 sites, enumerated above | MAJOR | instructor | open |
| DW-057 | 5 | **`session-0.1`'s disposition, held open by instructor decision.** It **stays in the repository, stays linked at `index.html:1045`, and is PERMANENTLY EXCLUDED from the Tier A / Tier B ungraded-repo purge**: it is not part of the Fall 2026 offering, it is a primer for a future course, so what it says about grading is a claim about an unscheduled course rather than a second source of truth against Canvas. It also **must not** be purged for a mechanical reason — its `LMBOX` and `LMSTYLE` fences are byte-paired with `session-1`'s, and editing either copy alone breaks the pairing (measured: `A9b` went from 6 shared blocks to 7). **Nothing here is a defect and nothing is owed now.** The open question is for whoever schedules that future course: at that point `session-0.1` becomes part of an offering, the exclusion's premise expires, and the file needs the same Tier A / Tier B pass sessions 1-4 have had — done to both fence copies in one commit, or not at all. Recorded so the exclusion is found before the course is scheduled rather than after. | `session-0.1/index.html`; `MAINTAINING.md`, the standing purge list | HOUSEKEEPING | instructor | open |
| DW-058 | 5 | **Ratchet lowered, recorded under the `MAINTAINING.md` ratchet rule.** The session-1 pass-2 deletion sweep removed six named blocks of prose from `session-1`, and the em dashes inside four of them went with them. `A9` session-1 entity **76 -> 72** on this cause: one in the s1 header's reveal note (*"Scrolls freely &mdash; nothing is withheld"*), one in section 07's tier gloss (*"looks trivial and is trivial &mdash; and it is the only item here"*), and two in the section 08 panel *"What you are looking at, and what you owe"* (*"Not a de-identified client &mdash; a constructed household"* and *"in any work you produce in this course &mdash; not in the workflow"*). THE DASHES WERE DELETED WITH THE PROSE THAT CARRIED THEM; none was converted under this row. The other two deletions (the *"Two notches past three"* pair in A1, and the trailing sentence of the cold-open *"Whatever you pasted"* paragraph) carried no dash. `session-0.1`, `session-2`, `session-3` and `session-4` did not move; `A9b` (R11) held at 6 blocks / 6 dashes, and no fence was touched. **Read with DW-059, which carries the other four of the same commit's eight.**  **Audited 2026-09-03:** record row; closes on the same ratification as DW-044. | `scripts/editorial-baseline.json`, `A9`; `session-1/index.html` | HOUSEKEEPING | instructor | open |
| DW-059 | 5 | **Ratchet lowered BY CONVERSION, not by deletion &mdash; the first such lowering in this file, and the reason it is a separate row from DW-058.** Every prior `A9` lowering (DW-044, DW-046, DW-047) states that the dashes were deleted with the prose that carried them and that *none was converted*. This one is the opposite case and must not be read as another of those: **the sentences were kept and only their punctuation was rewritten**, so the copy a student reads is unchanged in wording and four dashes still left the count. `A9` session-1 entity **72 -> 68** on this cause: three em dashes became colons (*"pasted exactly as you typed it:"*, *"no natural cutoff:"*, *"Approximation:"*) and one became a semicolon (*"add it as the next word;"*). No dash was added and none changed form from entity to literal, so `A8` holds at majority entity / minority 1. A fifth dash pair rewritten in the same pass moved NO figure and is recorded here so its absence from the numbers is not read as an omission: the parenthetical *"the surprising word &mdash; and the wrong citation &mdash; comes from"* at `session-1:2486` became round brackets, and it sits in an **R2 script string literal**, which `authoredProse` masks out. **The residue this leaves is EDITORIAL.md B6's, sharpened:** the ratchet now cannot distinguish a dash deleted with its sentence from a dash punctuated away while the sentence stayed, and only this register says which happened.  **Audited 2026-09-03:** record row; closes on the same ratification as DW-044. | `scripts/editorial-baseline.json`, `A9`; `session-1/index.html` | HOUSEKEEPING | instructor | open |
| DW-060 | 5 | **`EDITORIAL.md` B3's second worked example quotes a string that no longer exists, and the pass that changed it answered part of B3's own complaint.** B3 (*when a term is owed a definition*) cites `session-1/index.html:1322` and quotes *"Click a candidate to append it &mdash; the next distribution follows your choice"*, arguing that *`candidate`, `distribution`, `token` and `append` are undefined at this point*. The session-1 pass-2 sweep rewrote that label to *"Click a candidate to add it as the next word; the next distribution follows your choice"*, so **`append` is gone and three of the four named terms remain**. Two separate defects follow. (i) The quotation at `EDITORIAL.md:1134` is now stale and no `grep` of the lessons will find it. (ii) The line reference was **already** wrong before this pass - the label sat at `:1481` at `10fece6`, not `:1322` - so the citation has been drifting for longer than one commit and a re-check of B3's other line numbers is owed with it. Nothing was edited in `EDITORIAL.md`: rewriting a specification's worked example is outside a deletions pass, and the example may be worth keeping with the new wording rather than retiring. **The decision is which**: re-quote B3 against the current label and drop `append` from its list, or replace the example with one that still fails all four ways.  **Audited 2026-09-03:** `EDITORIAL.md:1131-1137` still quotes the old label; the label now sits at `session-1:1473`. | `EDITORIAL.md:1131-1137`; `session-1/index.html`, the §02 distribution picker | HOUSEKEEPING | instructor | open |
| DW-061 | 5 | **`MAINTAINING.md`'s Tier A / Tier B keep-list now names an example sentence that is no longer in the corpus.** The *What Tier A and Tier B do NOT reach* table's **Data-handling rules** row is justified by the sentence *"No client nonpublic personal information may appear in any work you produce in this course"*, described there as *a safety rule, not a grading rule; reworded off "submission", kept in force*. The session-1 pass-2 sweep deleted the section 08 panel that carried it (*"What you are looking at, and what you owe"*), on an explicit named instruction, so **the row's example is orphaned**. The RULE is not orphaned and the lesson is not left without one: the *Course rule* panel immediately above still carries *"No real client data or personally identifying information enters any AI tool at any point in this course &mdash; including the build transcript"*, which is the same prohibition in a stronger form and was untouched. So this is a citation defect, not a safety regression. **The decision is whether the deletion should stand at all**, because a keep-list is a ratification and this pass overrode one: either re-point the row at the surviving Course rule sentence, or restore the deleted panel. Recorded rather than resolved because the instruction to delete was explicit and the ratification is the instructor's to withdraw.  **Resolved by the 2026-08-29 Pass 2/Pass 3 merge:** Pass 3 had independently repointed the row at the surviving Course rule panel string (*"No real client data or personally identifying information enters any AI tool at any point in this course"*, `index.html:1103` / `session-1:1952`) on an explicit instruction in its brief — the first of this row's two options, taken before the row existed. The supersession note under the keep table in `MAINTAINING.md` records it. The deletion stands; the ratification question this row held open was answered by the instruction that ordered the repoint. | `MAINTAINING.md`, the standing purge list's keep table; `session-1/index.html` section 08 | HOUSEKEEPING | instructor | closed |
| DW-062 | 5 | **The cold-open ritual now opens differently in `session-1` than in the other four lessons, and no validator sees it.** *"Not a good one. Not a work one."* opens the last-prompt ritual in `session-0.1:1172`, `session-2:1396`, `session-3:1261` and `session-4:1289`; the session-1 pass-2 sweep deleted it from `session-1` on an explicit named instruction, leaving that lesson's ritual to begin at *"The last one, whatever it was"*. **Nothing catches this**: the five copies are R1 body prose, not R11, so they are not fence-paired, the md5 loop does not cover them, and A8/A9 count dashes rather than compare files. They were never byte-identical anyway - `session-0.1` reads *"typos and missing context and all"* with no em dash where the others read *"&mdash; typos, missing context, all of it"* - so the corpus already tolerated one variant, and this is the second. **The decision is whether the ritual's opening is a shared construct.** If it is, it belongs with the cross-session pedagogy held open as DW-050 and the deletion should propagate or revert; if it is not, four lessons carry an opening the flagship no longer does and that is simply the state. Scope kept this pass to `session-1`, so nothing was propagated.  **Audited 2026-09-03:** the opening line stands at `session-0.1:1149`, `session-2:1373`, `session-3:1238`, `session-4:1266` and is absent from `session-1`. | `session-1/index.html` cold open; `session-0.1:1172`, `session-2:1396`, `session-3:1261`, `session-4:1289` | HOUSEKEEPING | instructor | open |
| DW-063 | 5 | **A Part K retired fact is rendered at runtime by `session-1` §02, and no purge check in the repo can see it.** `CASE.md:820` lists the superseded structure's combined discount among the **"Grep targets that must return zero"**, and `CASE.md:816` retires its framing with *"Deleted. Do not reintroduce in any form."* `session-1:2437` prints every sampler candidate weight as a percentage — `'<span class="pv">'+(o[1]*100).toFixed(0)+'%</span>'` — and the second candidate list at `session-1:2421` opens with the weight `.31`. **The banned figure therefore enters the DOM as soon as the learner places one token.** Measured by driving the widget in Chromium and reading `document.body.innerText`: absent at step 0, present at step 1. **The adjacency is the defect, not the coincidence:** `CASE.md:813` retires exactly *"2023 appraisal supports a [retired figure] discount"*, the weight renders against the connector *", supported by"*, and the next list at `session-1:2422` offers *"a 2023 appraisal"* — the retired sentence is on screen in three clicks. Invisible to `grep`, to `git log -S` and to `verify-migration` check 1, all of which read source text, because the source holds `.31` and only render-time arithmetic produces the string. **Third independent proof of the PART 5 root cause** (after `session-3:2697` and the 23.4% figure) and the most serious: the other two are a disclosure gap and a stale weighting; this is a live Part K violation. Fix is a widget edit — move the weight off the banned value and renormalise `S2[1]` — so it was out of scope for the Pass 3 title/voice pass that found it. Note for whoever takes it: this register is **not** in `verify-migration.mjs`'s excluded-register set, so the figure must not be spelled here either.  **Renumbered on the 2026-08-29 Pass 2/Pass 3 merge:** this row was opened as DW-058 on the Pass 3 branch (commit `33f771b`, and `docs/session-1-feedback-inventory.md` §F-2.1 as first committed), but Pass 2 landed its A9-lowering row under DW-058 first and lowering rows keep their numbers under the ratchet rule, so this row takes the next free number. Unrelated to the phantom “DW-063” that §C-2 of the inventory cites from the never-committed measurement session.  **Closed 2026-08-29 by the commit directly after `78053ae` on branch `claude/session-1-card-sort-rebuild-nuzjoz`, titled *"The sampler weight that rendered a retired figure now rides the live discount"*** (the fix commit closes this row in the same change, so it cannot carry its own hash; the anchor commit, position and title identify it). `S2[1]`'s top weight now reads the injected `COLE.discount`, the live combined discount from CASE.md, with the remaining four weights renormalised to sum 1, so the render arithmetic can no longer produce the banned string and a CASE.md figure move flows into the widget instead of drifting past it. The gap this row named, that no purge check in the repo can see a render-time figure, is closed with it: `verify-browser.mjs` now drives the widget through all six distributions plus a reset in Chromium and asserts that no Part K render form appears in `document.body.innerText` at any step and that every rendered distribution sums to 100 ranked non-increasing, which also catches a future discount move that outruns the hand-typed tail. The banned strings are composed at runtime rather than spelled, so the script itself stays clean under `verify-migration` check 1. Negative-tested against the pre-fix tree: the drive reports the step-1 hit there and zero findings on the fixed tree. | `session-1:2421`, `:2437`; `CASE.md:813`, `:816`, `:820` | MAJOR | maintainer | closed |
| DW-064 | — | **September window. The Shift+U instructor override fires from inside text inputs, in every file that carries it.** Measured this turn: eight `keydown` handlers across the five session files act on Shift+U with no check of the active element. `session-0.1:1981` and `session-1:2418` share the GATES-block handler, which tests Shift+U and returns before its own `TEXTAREA`/`INPUT` guard eight lines below, so the guard protects the arrow keys and never the override; `session-2:3055`, `session-3:1870` and `session-4:2083` are per-lesson reveal handlers with no guard at all; `session-2:3098`, `session-3:2738` and `session-4:3309` are the shared gate-marking handler, also unguarded. The hub carries no override handler, which `verify-browser.mjs` documents as expected. A reader typing a capital U into any text input — the live-console key box included — reveals every withheld panel, the section 08 card-sort key among them. The fix must land across the carrier files in one commit to preserve ritual parity — the DW-043/DW-062 discipline: shared constructs move together or not at all.  **Audited 2026-09-03:** **reproduced in Chromium on all five lessons and raised to MAJOR.** With focus in the key box (`session-0.1`, `session-1`) or the cold-open textarea (`session-2`, `session-3`, `session-4`), Shift+U cleared every `.hidden` element (9 to 0, 8 to 0) and marked 12 / 18 / 16 / 18 / 18 gates; on sessions 2 to 4 the U also lands in the box. `session-2`'s first exercise is that textarea, so a student who types a capital U on 2026-09-14 reveals every answer panel. Handlers now at `session-0.1:1958`; `session-1:2400`; `session-2:3032`, `:3075`; `session-3:1847`, `:2715`; `session-4:2060`, `:3286`. Ranked first among mechanical fixes; land with DW-085. | `session-0.1:1981`; `session-1:2418`; `session-2:3055`, `:3098`; `session-3:1870`, `:2738`; `session-4:2083`, `:3309` | **MAJOR** | Claude Code | open |
| DW-065 | — | **September window. The lesson-builder skill's interleaving guidance carries no citation, and the corpus now holds the verified source it needs.** The component bank's design rule — `references/pedagogy.md` s3.5, *"the same mechanism twice in a row teaches the mechanism, not the content"* — is enforced by `references/build-checklist.md` item 5 and `SKILL.md`'s section-map interleaving check, and none of the three cites any source. The claim is the one `src-rohrer` carries — interleaving categories during classification practice beats blocking them — and that record was upgraded to verified/H on 2026-08-29 (ERIC EJ1071568; Journal of Educational Psychology, 107(3), 900–908; doi 10.1037/edu0000001). Amend the bank with the Rohrer 2015 record. **The fix belongs in the skill and the skill must not be edited from this repository** (the DW-029/DW-051 precedent): the guidance lives upstream, so the citation lands upstream, then syncs.  **Audited 2026-09-03:** the synced skill's `pedagogy.md` s3.5 still cites nothing. | upstream skill, `references/pedagogy.md` s3.5; `references/build-checklist.md` item 5; `SKILL.md`, the section-map interleaving check | MINOR | instructor | open-upstream |
| DW-066 | 4 | **September window. The session-1 cold open now renders the fixed eight-point prompt checklist, and the other four lessons still run the free-form analyser.** Pass 4 replaced the free-form response behind "Analyse what I typed" with eight deterministic presence checks, defined once in the `COLD_CHECKS` constant in session-1's family-17 block and commented as the course-wide cold-open checklist. The ritual markup (`coldPrompt`, `coldGo`, `coldOut`) is untouched; only the response changed shape, so pedagogy s3.8 ritual parity is owed: copy `COLD_CHECKS` and the renderer under it verbatim into the family-17 block of `session-0.1`, `session-2`, `session-3` and `session-4`, in one commit, the DW-043/DW-062 discipline. Until then the flagship diverges, the DW-062 shape, and DW-062's open decision about the ritual's opening prose rides along unchanged.  **Audited 2026-09-03:** `COLD_CHECKS` still only in `session-1`; `session-2:3042` runs the free-form analyser. Land with DW-074. | `session-1/index.html`, family 17, `COLD_CHECKS`; `session-0.1:1172`, `session-2:1396`, `session-3:1261`, `session-4:1289` | MINOR | Claude Code | open |
| DW-067 | 4 | **September window. The Wolfram verbatim-quote work is blocked from this environment: egress to `writings.stephenwolfram.com` is denied by the network policy.** Pass 4 was to replace two paraphrase reading boxes with the shortest exact spans from the assigned article ("About 50,000 tokens exist" in section 03, and the "his figures" n-gram box in A1) and to extend all six reading-box credit lines to name the article section each quote comes from, against the A11 locked list and toward A12/D6. Direct fetch, the harness fetch tool and the archive mirror all return policy denials, and no repo file carries the article text, so nothing was changed: a quotation mark must never wrap text that was not read from the source, the EDITORIAL.md Class A discipline. Fetch the article from an environment with open egress, verify each span byte for byte including its dash forms (A10 fires on any em or en dash inside an attributed quotation), then land all three sub-edits together. **Resolved 2026-08-29, against an instructor-supplied copy of the article.** The egress block this row records is still in force, so the article arrived as a file attached to the task rather than from the web, and it is the sole verbatim source for every span that landed. All three sub-edits landed in one commit, as the row required. §03's paraphrase became two quotations, each labelled with its own section: *“the” might be 914, and “ cat” (with a space before it) might be 3542.* from **The Concept of Embeddings**, and *about 50,000 values … only about 3000 of the tokens are whole words* from **Inside ChatGPT**. The A1 *his figures* box became two contiguous spans from **Where Do the Probabilities Come From?** joined by one ellipsis, and the elision is exactly the em dash in *1.6 billion—and the number of possible 3-grams*: that is the dash form this row warned about, and splitting the quotation there is what keeps A10 clean and the five figures verbatim at the same time, so no halt was needed. Five of the six credit lines now name their section. Verified by `scripts/verify-wolfram-quotes.py`, added in the same commit, which normalises whitespace, quote forms and HTML entities on both sides, splits every quotation at its ellipses and asserts each fragment is a contiguous substring of the article; all five new spans verify EXACT and none carries an em or en dash. **One pre-existing misquote was found and deliberately NOT corrected:** `session-1:1493` reads *“which could be just part of a word.”* where the article reads *which could be just a part of a word* — the indefinite article was dropped. Repairing a quotation is not what this row authorised, so that box and its credit line stand byte-unchanged and the defect is raised as **DW-068**. Two side effects, both recorded rather than absorbed: A9 fell 69 to 67, the two dashes having sat in the §03 gloss the quotations replaced, so `scripts/editorial-baseline.json` was lowered by hand under MAINTAINING.md's re-baselining procedure, which `test-editorial-regions.mjs` T7 pins; and A12 fell from 8 advisories to 7, the A4 box now reaching a section name, which is the D6 direction this row was working toward. | `session-1/index.html`, the six `div.wolf` reading boxes, sections 02, 03, A1, A3, A4 | MINOR | Claude Code | closed |
| DW-068 | 4 | **A quotation in `session-1` §02 drops a word from the assigned article.** The second reading box in section 02 renders *“which could be just part of a word.”* Wolfram writes *which could be just a part of a word*: the indefinite article **a** is missing, and the source has a comma where the lesson closes the sentence. Found by `scripts/verify-wolfram-quotes.py` while landing DW-067, which flags it as the one span in the six reading boxes that is not a contiguous substring of the article; the other ten spans verify. DW-067's brief said to flag a misquote and leave it unchanged rather than guess, so the box was not touched. The fix is one word and needs no new source, but it changes a quotation, so it wants an instructor's eye and its own commit. Note the box's other span verifies EXACT, so only the second quotation moves. **Closed 2026-08-29 by the follow-up commit on `claude/dw-067-wolfram-quotes-5zzqxl`.** The missing **a** was inserted, and the sentence-closing period moved outside the quotation marks, because the article reads *a part of a word,* with a comma: leaving the period inside would have traded a dropped word for an added one and left the span verifying only EXACT-1. **Both period-outside-quotes edits, the one at 1493 and the one at 1637, were ratified by Jared on 2026-08-29.** **Also ratified by Jared on 2026-08-29:** the `scripts/verify-wolfram-quotes.py` credit-line matcher extension to `(?:wh|src)` made in commit `8679a7c`. That commit moved the A2 credit line from `span.wh` to `span.src`, and the verifier had located the paragraph by its `wh` credit line and stripped that line before extracting spans, so the swap alone dropped both 1637 figures from the run and took the tally to 13 exact while still exiting 0. The matcher now accepts either class, over the same spans as before. The brief for that commit had put the verifier out of scope, so the edit was made against it to hold the stated tally and reported for ratification rather than assumed. It now verifies EXACT, and the credit line was left unchanged as the brief required, which leaves this the one reading box of the six whose credit line still does not name its section. **The same commit also resolved a defect that never had a row:** the `p.dim` at `session-1:1637` quoted *about 100 billion neurons* and *maybe 100 trillion connections* as one "Wolfram's comparison" with no credit line at all, though the two spans come from different sections of the article, **Neural Nets** and **What Really Lets ChatGPT Work?** respectively. It now carries a credit line naming both, both spans are byte-verified and `scripts/verify-wolfram-quotes.py` checks them. Two things that paragraph still owes, neither in this commit's scope: its THIRD quotation, *a bit less than one neural net weight to carry the information content of a word of training data*, drops the article's own quote marks around *information content* and does NOT verify, so the verifier whitelists the two credited figures rather than reporting it clean; and `.wh` is styled `.wolf .wh` inside the managed style fence, so the new credit line renders unstyled in a `p.dim` and reaches A12 as R1 rather than as a source note. | `session-1/index.html:1493`, the second `div.wolf` in section 02; `session-1/index.html:1637` | MINOR | instructor | closed |
| DW-069 | 4 | **September window. A third quotation in the `session-1` A2 paragraph drops the article's own quotation marks, and the verifier does not check it.** `session-1:1637`'s `p.dim` renders *it takes "a bit less than one neural net weight to carry the information content of a word of training data"*. Wolfram puts *information content* in quotation marks of his own, so the article reads *a bit less than one neural net weight to carry the “information content” of a word of training data.* The lesson's span is therefore NOT a contiguous substring of the source: it is a quotation inside a quotation, flattened. Raised while closing DW-068, whose commit credited the paragraph's other two figures; this one was deliberately left alone because repairing a quotation was outside that brief. `scripts/verify-wolfram-quotes.py` **whitelists it rather than verifying it** — `PDIM_FIGURES` limits the paragraph's checks to the two figures the credit line attributes, and this span is reported as `[skip]`, so the run's 0 FAILED does not mean this string was read. Fixing it means restoring the inner marks in whatever nesting form the box uses, then **byte-verifying the result against `wolfram-article.txt` including the article's own quotation-mark characters**, since it is precisely those characters that are missing; a check that normalises them away cannot see this defect. Take it out of the whitelist in the same commit, so the span is verified rather than skipped.  **Audited 2026-09-03:** still flattened, now at `session-1:1614`; `PDIM_FIGURES` unchanged. | `session-1/index.html:1637`, the third quotation in the A2 `p.dim`; `scripts/verify-wolfram-quotes.py`, `PDIM_FIGURES` | MINOR | instructor | open |
| DW-070 | — | **September window. Every figure drawn inside the Part L sheets is a hand-typed literal, and no validator ties any of them to CASE.md data.** The v4.0 pipeline pins the sheets as bytes, not as facts: `verify-case.mjs` hashes the injected block whole, `verify-browser.mjs` counts *2 SVG, 2 title, 2 desc* and checks the stamp format, and `build-case.mjs`'s `grab()` patterns never read Part L, so a wrong number drawn into the artwork ships through every check in the repo. The motivating instance shipped for two audits: until the 2026-08-30 redesign (CASE.md Part N v4.4), Sheet 2's *"428 units pro rata · $2,140,000"* ribbon was drawn at `stroke-width="3.5"` against a shared scale near 5.6 px per million that demands about 12, directly under its own caption *"ribbon width ∝ annual dollars"* and beside a key that paired 3.5 with $2,140,000 and 11.7 with the smaller $2,095,236. The redesign fixed the instance and shrank the class, not the risk: the detail layer is now generated from `case-facts.json` by `scripts/inject-case.mjs`, but the sparse default views still carry hand-typed literals (up to six dollar figures per sheet, the unit counts, the rates, and the six ribbon `stroke-width` values), and before the redesign figures such as the trust's $2,860,000 pro-rata share and the $2,095,236 purchase stream had no `case-facts.json` key at all. **Fix = add case-facts keys for the remaining unkeyed derived figures and a pinning check** that parses each sheet's `<text>` content plus the ribbon widths and asserts every dollar figure, unit count, percentage and width-to-dollar ratio against `case-facts.json`, the way `verify-migration.mjs` check 20 pins prose. This row records the gap; the validator is deliberately not built here.  **Audited 2026-09-03:** measured: 11 dollar figures are drawn in the two sheets and **1 is unkeyed** (`$2,860,000`); the 2 unit counts are keyed; 9 distinct `stroke-width` values. The empty phase column is filled with the dash the other out-of-phase rows use. | `CASE.md` Part L; `scripts/case-flowchart.html`; `scripts/build-case.mjs` `grab()`; `scripts/verify-case.mjs`; `scripts/verify-browser.mjs` flowchart checks | MINOR | instructor | open |
| DW-071 | — | **Audit 2026-09-03. The hub asserts a sentence its own annotation calls wrong.** `index.html:1081` reads *"Every example across all five sessions uses one synthetic household, the Cole case, constructed for this course. [UNCONFIRMED]"*, and the CLAIM annotation beside it reads *"Narrow it to the claim that is true and equally reassuring: every CLIENT example uses one synthetic household. As written it is wrong in both…"*. The hub is the page every session opens from, and the resolution is already written in the annotation: narrow the sentence to client examples and drop the marker. `docs/unsourced-claims.md` carries it at weight `claim`; `verify-editorial` A16 sees only the marker's form. | `index.html:1081` | MAJOR | Claude Code | open |
| DW-072 | — | **Audit 2026-09-03. `session-1` §02's rank-probability chart is a constructed harmonic series presented as a measurement.** `docs/session-1-feedback-inventory.md` §D-3 recomputed the page's 23.4 and 2.3 per cent as rank 1 and rank 10 of a pure n⁻¹ series over 40 tokens (H(40) = 4.278543, exact), and §F-4 measured that the page does not say so: the chartbox at `session-1:1501` carries no `.sim` label, the nearest one (`:1477`, *"Illustrative distribution"*) belongs to the picker panel that closes at `:1486`, and `:2531` reads *"the power law Wolfram identifies as characteristic of language"*, so the only confidence a reader can attach is the H chip resolving to Wolfram. A reader could repeat 23.4 per cent as a measured figure. Inventory §E item 7 leaves the fix as an instructor decision: label the chart illustrative (a `.sim` caption, minutes), or replace it with real softmax output. The page is live; Session 1 has been taught. | `session-1/index.html:1501`, the §02 chartbox; `:2531` | MAJOR | instructor | open |
| DW-073 | — | **Audit 2026-09-03. Eleven of the thirteen marked claims in `docs/unsourced-claims.md` have no row.** The generated register lists every `[UNCONFIRMED]` and `[NEEDS SOURCE]` on the served pages; this register carries rows only for `session-4:1563` (DW-006) and `session-3:2249` (DW-013). The other eleven, by the weight the generated register assigns: **answer** — `session-4:1724`, vendor model-card figures duplicated into the Appendix D3 quiz feedback strings, so a student is keyed against an unconfirmed figure; **section** — `session-2:1576` `[NEEDS SOURCE]`, Anthropic's claim of removing more than 80 per cent of a system prompt without measured loss, on the page taught 2026-09-14; `session-4:1501`, `:1525`, `:1658`; **exercise** — `session-4:1557`; **claim** — `session-3:1585` (the recording-consent compilations), `session-4:1616` (two statutes' dates); **aside** — `session-4:1525`, `:1601`. Each resolve note on the page states what would close it. Take `session-2:1576` first, then the answer-weight item. | `docs/unsourced-claims.md`; eleven sites, enumerated above | MAJOR | instructor | open |
| DW-074 | — | **Audit 2026-09-03. Sessions 2, 3 and 4 still carry the instructor-addressed pacing block that Pass 3 rewrote in `session-1`.** *"Instructor minute budget"* heads the block at `session-2:1985` and once each in `session-3` and `session-4`; `grep -c` returns 0 in `session-1`, whose block `a5ede33` rewrote to describe two paths to a reader with every minute read from the footer loop. The pattern exists; propagate it to the three lessons in one commit, `session-2` first. | `session-2/index.html:1985`; `session-3`, `session-4`, the same heading | MINOR | Claude Code | open |
| DW-075 | — | **Audit 2026-09-03. In-class deixis purged from `session-1` survives in the other four lessons, now measured.** Pass 2 took `session-1` to zero on *tonight* and *today* (`acd691f`). The counts elsewhere: `session-2` **2 / 5** (`:1382` *"Tonight you learn the framework"*, `:1977`; `:1581`, `:1937`, `:1942`, `:2732`, `:2947`); `session-3` **19 / 2**; `session-4` **17 / 6**; `session-0.1` **6 / 4**. This closes `docs/session-1-feedback-inventory.md` §D-2's *"never been measured"*. Whether the `session-1` standard is corpus-wide is the decision (the Pass 2 instruction named one file); once taken, the edit is mechanical and lands per lesson. | `session-2/index.html`, seven sites; `session-3`, `session-4`, `session-0.1` | MINOR | instructor | open |
| DW-076 | — | **Audit 2026-09-03. `session-3` carries an instructor-directed panel label of the shape DW-046 removed from `session-4`, and the same panel holds DW-002's false sentence.** `session-3:2590`, a revealed student-facing panel in a script string, is headed *"Complication — instructor introduces, nobody re-speaks"*: an explicit instructor marking carrying delivery direction only, which is the Phase 4 definition. Its body is the *"scored zero and never appeared"* sentence. Land the relabel with DW-001/002/003 in one commit. | `session-3/index.html:2590` | MINOR | Claude Code | open |
| DW-077 | — | **Audit 2026-09-03. At 380px the Case facts control overflows the viewport on the two console-carrying pages.** Measured in Chromium: `#caseBtn`'s right edge is at **472px** in `session-0.1` and **456px** in `session-1` on a 380px-wide viewport (92px and 76px past the edge); the hub and sessions 2, 3 and 4 fit (365 to 366px). `docs/session-0-1-progress.md` FLAG-B8-TOPBAR recorded it as corpus-wide; it is the two pages whose topbar also carries the Live-model control. The topbar CSS is inside the managed `STYLE` fence, so the fix is a media query upstream in the skill, or a lesson-local override outside the fence in both files in one commit. | `session-0.1/index.html`, `session-1/index.html`, `#topbar` at 380px | MINOR | Claude Code | open |
| DW-078 | — | **Audit 2026-09-03. `README.md` still says the Session 1 baseline and the Session 2 prompts are graded components.** `README.md:29-31`: *"The baseline measurement in Session 1 and the prompts you rewrite in Session 2 are graded components of the final project"*, against the 2026-08-27 decision recorded in `MAINTAINING.md`'s purge list (*"Nothing in this repository is graded and it never will be"*). README sat outside the purge population, which was the hub and the lessons. A smaller residue in the same file: `README.md:3` *"Fall 2026"*, which the hub no longer says (DW-009, closed). Reword to the rule the site follows: the site names no grade; the work is yours to keep. | `README.md:3`, `:29-31` | MINOR | Claude Code | open |
| DW-079 | — | **Audit 2026-09-03. `README.md` carries four `[UNVERIFIED]` instructor facts on the repository's landing page.** The session table reads `[UNVERIFIED: session 3 date]`, `[UNVERIFIED: session 4 date]` and `[UNVERIFIED: session 5 date]`, and the last line reads `[UNVERIFIED: preferred instructor contact]`. Only the instructor holds these. If the site's no-calendar rule extends to README, delete the column instead. | `README.md`, the session table and the last line | MINOR | instructor | open |
| DW-080 | — | **Audit 2026-09-03. The verification surface cannot run from a clean clone as documented.** `scripts/verify-style.mjs:34` hard-codes a skill path that does not exist on this host (synced skills now live under a uuid-prefixed directory), so the script exits 1 unless `RESTYLE_SWEEP` is set; `scripts/build-changelog.py` already solves the same problem with an env var plus a candidate list, and the pattern should be shared. `scripts/verify-wolfram-quotes.py` needs the article text as its argument, no copy is in the tree (egress is blocked), and `MAINTAINING.md` names neither the script nor its input, so the check that guards DW-069 is in no documented gate. `scripts/state_space.py` is named nowhere. `docs/session-0-1-progress.md:18` and `docs/editorial-gap-report.md:2017` carry the stale path. | `scripts/verify-style.mjs:34`; `scripts/verify-wolfram-quotes.py`; `MAINTAINING.md`, the gate lists | MINOR | Claude Code | open |
| DW-081 | — | **Audit 2026-09-03. The moving-target sources are ageing into September and no row owns the re-pull.** `docs/source-verification-queue.md` at `8442160`: 21 moving targets; **four with no retrieval date** (`src-synthid`, 10 references; `src-vectara`, 4; `src-owasp`, 1; `src-kitces-advisortech`, 0); `src-aa` retrieved 2026-08-13, 21 days old, behind 12 references including two in `session-2` `#s5`; `src-anthropic-terms` 2026-08-14; `src-charlotin` month-only, behind `session-2:1781` *"1,598 as of 9 June 2026"*. The fourteen help-centre pulls of 2026-08-20 will be 25 days old on 2026-09-14. `DATA-PULL.md` states the procedure and the three precondition failures; nothing says when it runs. Decide the cadence (before each session is the obvious rule) and who pulls where egress allows. | `SOURCES.md`, the 21 `moving_target` records; `DATA-PULL.md` | MINOR | instructor | open |
| DW-082 | — | **Audit 2026-09-03. `docs/repo-updates-plan.md` Phases 5, 6 and 7 were never executed and are tracked nowhere.** The plan's §0 table gates them after Phase 3; §13, §15 and §16 record Phases 2, 3 and 3.5 as executed and `CHANGELOG.md` records 3.6 and 4; nothing records 5, 6 or 7. What they owe, in the plan's own figures: §6, the Wolfram citations under the D7 rule (*"Twelve references are auto-applicable. Eighty-six go to the instructor"*), the `data-term` marking, the planning-analogue injections (*"57 of the 96 bridge sentences do not exist in a usable form"*) and the Monte Carlo relocation; §8, the vocabulary inventory, with A17 to A19 `DISABLED` and `verify-editorial.mjs` printing *"not built"*; §9, three decisions. The session-1 passes of 2026-08-28/29 ran from the feedback inventory, not from this plan, so nothing in it is ticked. Decide whether Phases 5 to 7 remain the plan; if not, retire them in §0 so this register is the only list. | `docs/repo-updates-plan.md` §0, §6, §8, §9 | MINOR | instructor | open |
| DW-083 | — | **Audit 2026-09-03. `CHANGELOG.md`'s newest entry describes a branch as unmerged that had merged the same morning, and PR #17's own work has no entry.** `CHANGELOG.md:15`: *"unmerged on purpose: parked for Jared to merge after his first class session"*; PR #18 merged it at `8442160` on 2026-08-30 10:13 -0500, the day before the class. The changelog catch-up itself (`605d34d`), the provenance register (`3585d0e`) and their merge (`dc8f109`, PR #17) appear nowhere in the file. The file's own rule keeps old entries unedited, and the 2026-08-29 audit left an errata-entry mechanism *"under consideration for September"*: decide it, then the two corrections are one forward-dated entry. | `CHANGELOG.md:15`; the missing PR #17 entry | HOUSEKEEPING | Claude Code | open |
| DW-084 | — | **Audit 2026-09-03. Two contested rulings in `audit/AUDIT-2026-08-29.md` are waiting on the instructor.** The findings on `746eba9` and `d21f0eb` (ratchet lowerings the changelog never recorded) are held at Cosmetic *"on a call that is open to reversal"* and *"move to Material if he takes the other side"*; 28 findings in that file have no sanctioned remedy until the errata mechanism (DW-083) exists. Rule on the two; the rest follow the mechanism. | `audit/AUDIT-2026-08-29.md`, the CONTESTED section | HOUSEKEEPING | instructor | open |
| DW-085 | — | **Audit 2026-09-03. The Shift+U override label reads five different ways, and `session-0.1` has none.** `#ovr` text: `session-1` *"Shift+U — reveal all"*; `session-2` *"Shift+U — reveal all answer panels"* (set by script); `session-3` *"Shift+U — instructor override"*; `session-4` *"Shift+U · reveal all"*; `session-0.1` carries no `id="ovr"` element. `README.md` describes *"The Shift+U label in the top bar"* as one thing. A shared construct; land one wording in five files with DW-064's handler fix. | five lessons, the topbar override label | HOUSEKEEPING | Claude Code | open |
| DW-086 | — | **Audit 2026-09-03. Three lists of open work still live outside this register.** (a) `docs/session-1-feedback-inventory.md` §E items 4 (PART 7 gates the §00 / §01 / §07 rebuilds), 5 (retire the 845 figure; a residual of 1 unexplained), 6 (deixis for sessions 2 to 4, now DW-075) and 7 (the chart disclosure, now DW-072). (b) `EDITORIAL.md:1604-1618`, five follow-ups, of which *"Report §6 and §7 still need re-deriving"* and the RC-1 to RC-3 numeric defects are recorded nowhere else. (c) `audit/AUDIT-2026-08-23.md` Part 10 DF1, the 13-interaction core floor that sessions 1 and 2 sit below, *"deferred at the instructor's direction"* and never given a row, so the next reader re-raises it. Fold (a) and (b) into rows or pointers here; record (c) as the decision it already is. | `docs/session-1-feedback-inventory.md` §E; `EDITORIAL.md:1604`; `audit/AUDIT-2026-08-23.md` Part 10 | HOUSEKEEPING | Claude Code | open |

## Superseded

Kept so they are not raised again as though new.

| id | phase | description | file:line | severity | owner | status |
|---|---|---|---|---|---|---|
| DW-011 | 3.5 | `disclose_on_page: true` has no consumer. `inject-sources.mjs`'s `renderEntry()` never emits `rec.scope`, so the synthetic-case disclosure is off all four lesson footers. §16.6(c) states the three options and says DO NOT REVERT. | `scripts/inject-sources.mjs`, `renderEntry()` | — | — | superseded — closed in Phase 4. `renderEntry()` now emits `rec.scope` gated on `rec.disclose_on_page === true`, which is §16.6(c)'s first option; no `scope` value in `SOURCES.md` was changed. `verify-sources.mjs` checks the gate in both directions and both are hard failures. |
| DW-015 | 3.5 | `verify-migration` check 1's grep targets missed two retired framings that survived the v4.0 migration — the bare word *founder* applied to Meg, in three lessons, and *9-year* / *nine-year* applied to the note, in two. Both were corrected by hand; the check still cannot see either. | `scripts/verify-migration.mjs` | — | — | superseded — closed in the ungraded-repo turn. **Check 1b** now inverts the question: instead of asking whether a PERMITTING subject is within 240 characters, as check 4 does, it asks whether the FORBIDDEN one is within 160. Both are needed — check 4 passes a match as soon as any permitting word appears nearby, so a paragraph about Walter's 2016 purchase that also calls Meg the founder passes it. R6 and R10 are excluded by offset rather than by filename, so session-0.1's captured AFR transcripts keep their verbatim *9-year*. Negative-tested both ways: 0 hits on the tree, exit 1 on each planted string. |
| DW-016 | 3.5 | `verify-migration` check 4's conditional-allowance test matches `18,000,000` and not `$18M`, so an abbreviated case figure carrying the wrong label passed it. | `scripts/verify-migration.mjs` | — | — | superseded — the `$18M` pattern was added in Phase 3.5 (`d21f0eb`-era, the block commented *THREE ADDED 2026-08-25*) and the row was never closed. Verified by negative test rather than by reading: planting *"a company appraised at $18M"* in session-1 makes check 4 exit 1 naming *the F.6 buy-sell formula output, abbreviated*. Zero occurrences on the current tree. |
| DW-034 | 3.5 | `session-1` §10 teaches a price rise the vendor has publicly cancelled, on a course dated the night before it was supposed to happen. | `session-1/index.html:1875`, the surviving row | — | — | superseded — closed in Phase 3.6 |
| DW-035 | 3.5 | The `session-3` retrieval corpus's buy-sell chunk describes a right of first refusal with a descendants'-trust carve-out where §F.6 gives a corporate consent gate over every transfer. | `CASE.md` Part O chunk D2, moved there from `session-3` in the same phase | — | — | superseded — closed in Phase 3.6 |
| DW-036 | 3.5 | `src-synthid`: eleven references, no link, no publication date, no retrieval date, no reachable page. | `SOURCES.md` | — | — | superseded — one reference closed by `src-synthid-text`; the remaining ten are DW-005 |
| DW-037 | — | `MAINTAINING.md`'s follow-up list says *"D14 is unanswered."* `EDITORIAL.md` answers it as **D20**, 2026-08-25. | `MAINTAINING.md` | — | — | superseded — the list entry is stale, not the decision |
| DW-038 | — | `MAINTAINING.md`'s follow-up list says *"48 Part A violations stand, pending the D16 burn-in."* `verify-editorial.mjs` reports **0 hard failures and 13 advisory** on the current tree. | `MAINTAINING.md` | — | — | superseded — re-measure before re-raising |
| DW-039 | — | `MAINTAINING.md`'s follow-up list says *"27 of 193 confidence chips resolve to the wrong source."* Phase 3 rewired them; the standing measurement is now A13 and A15 on every run. | `MAINTAINING.md` | — | — | superseded |
| DW-040 | — | `MAINTAINING.md`'s follow-up list says `session-1`'s named discussion block is 17 minutes against a ratified 15. Closed by **DEC-2 / DEC-3**: restored to 15, the two minutes returned to `#s8`. | `MAINTAINING.md` | — | — | superseded |
| DW-048 | 5 | **One Tier A string cannot be purged from session-1 without editing session-0.1.** `session-1:1349` reads *"Nothing is graded on it and nothing breaks without it."* It sits inside the `LMBOX:BEGIN`/`LMBOX:END` console fence, whose own comment says *keep byte-identical across lessons* and which `MAINTAINING.md`'s pre-push gate md5-pairs against session-0.1. session-0.1 is out of scope for the ungraded-repo purge, so removing the clause from session-1 alone broke the pairing and raised `A9b` from 6 blocks to 7. **The edit was made, measured, and reverted**; the string stands. Three ways out, all the instructor's: edit both copies together in one commit; move the sentence outside the fence in both; or accept it, since it disclaims grading rather than asserting it. | `session-1/index.html:1349`, inside `LMBOX` | — | — | superseded — **accepted as-is by the instructor, 2026-08-27.** The sentence *"Nothing is graded on it and nothing breaks without it"* **disclaims** grading rather than asserting it, so it is not the defect Tier A exists to catch, and editing it would cost the `session-0.1` byte-pairing for no gain. None of the three ways out was taken and none is owed. Recorded in `MAINTAINING.md` next to the purge-list entry. Do not re-open. |

---

## Adding a row

1. Take the next id. **Never reuse one and never renumber.**
2. Record the phase that **raised** it, so the register reads as history as well
   as as a queue.
3. Quote the string, not only the line number. Line numbers drift on every
   reflow; the string is what someone greps for two phases later.
4. Owner is what the work needs, not who is senior. If it needs a judgment, it
   is the instructor's.
5. A row is **superseded**, never deleted. A deleted row gets re-raised by the
   next person who notices the same thing.
