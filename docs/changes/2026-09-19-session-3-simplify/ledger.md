# Ledger: Session 3 simplify pass, 2026-09-19

Branch `claude/adoring-mendel-2luer4`, from `7d9e2fe` (`main` after PR #32). One row per atomic
item; ids run `SM-001` upward. Anchor is section id plus `data-task` where one exists, plus a quoted
string. `checks.mjs` in this folder carries one DOM assertion per item where a DOM check can prove it.

Baseline measured before any edit: T7 `session-3 4 literal / 3 entity`; 18 interaction roots;
17 gates; 2 `.verify` blocks; the four §03 presets in Chromium at D1 11.1 / D6 5.1 / D3 3.3 / D2 0.0
(53.8%), D5 18.2 / D4 17.9 (1.7%, flagged), D8 29.2 / D3 8.0 / D7 3.8 / D1 3.7 (72.8%), D7 26.5
then zeros (100%); no horizontal overflow at 380px at either depth; zero page errors; every repo
gate green and the skill validators at the seven by-design red lines.

| Id | Anchor | Planned edit | Test | Commit | Result | Status |
|---|---|---|---|---|---|---|
| SM-001 | `docs/changes/2026-09-19-session-3-simplify/` | Open the change folder: the kickoff verbatim, this ledger, `checks.mjs` with the standing invariants | `node checks.mjs` runs, 0 failed | | | open |
| SM-002 | `s12`, "This section characterises recording law and a professional duty. Verify before teaching either as settled" | The §09 verification block leaves the page; its two items go to the run sheet under Verify before teaching | `.verify` count 0 | | | done |
| SM-003 | `#caseInner`, after `CASE:END`, "Every tax and legal characterisation attached to this case is posed as a question" | The shared case gate leaves the page (Session 2 precedent, DW-056 batch 3); its four items and the Karmazin, Woelbing and Davidson line go to the run sheet | no "Requires instructor verification" on the page | | | done |
| SM-004 | `#caseInner p.dim`, "Generated from CASE.md, the canonical fact set" | The case dialog's lede stops naming files and scripts and says what the dialog is for | the lede names no file or script | | | done |
| SM-005 | `instructor-notes/session-3.md`; `docs/deferred-work.md` DW-056, DW-041 | A Verify before teaching section carries every item the two gates held plus the §07 excerpt disclosure; the §09 slot and the not-yet-done list point at it; both register rows carry a dated line | reads true against the page | | | done |
| SM-006 | `s10` `.src`, "The excerpt is CASE.md Part O; it presupposes an advisory engagement" | The maintainer aside and two file references leave the §07 source line; the DW-041 disclosure moves to the run sheet | source line names no file | | | done |
| SM-007 | `sChk` `.src`, "written for this lesson from CASE.md Part O; lines 3 and 5 are deliberately wrong" | The A3 source line stops naming the file and stops printing the two planted lines under the sorter built to find them; the planted-error label stays | no file name, no line numbers, label kept | | | done |
| SM-008 | `s15` `t-s15` and its script: "The protocol.", "Defence phase: 7 to 9 minutes, minority called first", "The room distribution stays hidden", "you supplied evidence for the other by finding the Article VII failure yourselves", "epistemic move", "held the right answer" | Appendix C4 reads for a learner alone: the method replaces the facilitation protocol, the defence panel and the locked readout stop promising a room distribution the page never draws, the reader is not told they personally found Article VII, and the appraisal is described as the document that prices the company rather than the one that held the right answer | after vote one, no stage direction in `#s15` | | | done |
| SM-009 | `sOff` `t-sOff`, OFFI items 1, 3, 4; "No claim is made about any vendor"; silent-run step 3; "Click a task, then a column"; "[Adviser]" | §08 agrees with the summary it runs on (no "three decisions", no "one decision per slide"), the sorter item stops presupposing units, the reviewer-facing disclaimer leaves the source line, step 3 tells a learner with no Claude or a stalled run to open the example output, the idle text says bucket like every other sorter, and the example letter signs off Advisor | four steps, no contradiction strings | | | done |
| SM-010 | `sPrep` `t-sPrep`, MEET, the bracketed hint, "your own tool", "your next meeting", "Pick the areas" | The prep builder offers a third meeting type for the three who do not run client meetings, the bracketed builder note goes (the third bullet and the prompt both say it), the hint names Claude instead of "your own tool", the work-along stops assuming a scheduled client meeting, and the first bullet takes the page's label form | three meeting buttons; no bracketed note | | | done |
| SM-011 | `s1`, "The file." bullet, "Tonight." bullet, bridge hint, the h4, BRIDGE B | §00 carries a figure-free four-line panel on the Coles (household with David, Nathan and Claire; the plan with the seed gift, the sale for a demand note and the asserted discount; the documents; what is new), so every later interaction has its facts on the page; the Tonight bullet names grounding; the bridge hint says the four checks are from Session 2; the h4 takes title case; one hyphenated adverb goes | panel present, names present | | | done |
| SM-012 | `s4` `t-s4`, the lede, Index and Rank bullets, "Ten passages", "Where it fails", the hint, the preset button text | §03 says RAG and grounding are one thing, keeps the word embedding instead of a second metaphor, reconciles nine documents with ten passages, stops announcing before the commit that two presets fail, prints each preset question on its button so the commit can be made before the click, and tells the learner the one fact the judgement needs: Meg is selling shares to a trust. PRESET q strings untouched; presets identical | button text; hint; presets unchanged | | | done |
| SM-013 | `s2` `t-s2` CSNOTE, `t-s3` PAIRS, "Company, not meaning.", work-along g3 | The three confusable-pair notes and four of the six pair explanations say what they mean without the case: no IDGT, which trust is which, what the discount is and where it is asserted, which return the wrong figure reaches, why the will and the trust both receive the residue; the unsupported "nearly the same person" clause goes; the embeddings bullet parses on first read; the work-along says read the key, which opens itself | no IDGT on the page | | | done |
| SM-014 | `s9` `t-s9`, the hint; CHAIN Prep `v`; `instructor-notes/session-3.md` s9 slot | The §06 question has one answer (two stages are never seen and one is seen at examination, so "found last" had two defensible answers): the stage whose skipped check is first seen at an examination, still 5; the Prep panel stops presupposing a remark the learner meets one section later; the run sheet asks the same question | hint text; answer unchanged | | | done |
| SM-015 | `s10` `t-s10`, the excerpt heading, the hint, STAGES 1, 2 and 4, "Why the category exists", work-along g8 | §07 says who is in the room (the advisor with Meg and David Cole), the transcription stage prints stamps a tool would emit, the extraction stage says Nathan is their son and states the hypothetical drop in one breath, the follow-up stage obeys the not-given rule §08 teaches and flags the commitment the tool added, the hint stops repeating the sim label, and the work-along stops presupposing a note-taker | stamps, names, flags | | | done |
| SM-016 | `s12` `t-s12`, the Confidentiality bullet, the hint, work-along g9, QITEMS feedback, the source line | §09 says who Nathan is where the quiz turns on him, poses the vendor as a supposition rather than a fact about the learner's firm, drops "Cole item" for a quiz where two items are not Cole items, asks the state question in a form a learner can answer or look up, calls item 1 the hardest rather than "built to split the room", and writes the section sign as an entity like the rest of the page | strings present and absent | | | done |
| SM-017 | `s13` `t-s13`, DOCI line 3, "Copy it into your own tool", work-along g10, the copy button | The basis example no longer records a client agreement in a review whose summary records no decision, nor a reason counsel never gave; the prompt is copied into Claude after the next meeting the learner writes up; a learner with no meeting runs it on the Cole review; the button reads like the others | strings | | | done |
| SM-018 | `s16` `t-s16`, CKL item 3, the copy handler | Checklist item 3 says "the instruction given to the tool" rather than the unglossed "standing instruction"; the work-along completes when the list is copied out, not only when all ten are ticked, since an honest beginner rarely ticks ten | copy marks g12 | | | done |
| SM-019 | `s6`, "In the Cole file." bullet; HALL row 2; TUNE feedback | §04 states Article VII's trigger as the passage and the §03 verdict state it (death, disability or withdrawal); the §04 chart row spells out Westlaw AI-Assisted; §05's feedback says retrieval tool, the page's term, not pipeline | strings | | | done |
