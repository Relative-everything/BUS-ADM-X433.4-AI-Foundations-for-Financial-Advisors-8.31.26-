# Plan: Session 2 pre-flight for 2026-09-14
From: direct request ("fix what you can before my manual pass tomorrow"). Date: 2026-09-12. Tier: Lite (changes inside an existing surface; every design decision was taken 2026-09-08 as D1 to D9 or specified in `audit/AUDIT-2026-09-07.md` §5.5, §6 and §12.4).
Status: accepted (taken from the builder's instruction of 2026-09-12; see Flags in handback.md, F-A).

## Reads
- `docs/audits/session-2-review-2026-09-11.md`: the 140 findings this plan works from, with line anchors at 98e9686.
- `audit/AUDIT-2026-09-07.md` §5.3 (run order), §5.4 (corrections slot), §5.5 (fixture pack), §6 PR B/C/D, §12.4 (the 16 line-anchored edits): the decided edit texts.
- `docs/deferred-work.md` header: D1 to D9 taken 2026-09-08; PR B/C/D unblocked; DW-050 deferred; rows DW-087 to DW-101.
- `MAINTAINING.md` "Sources", "The editorial checks", "The appendix reflow", "Publishing": footers regenerate from `SOURCES.md` via `inject-sources.mjs`; the em-dash ratchet is a halt in both directions unless Jared asks for the figure; generated regions are never hand-edited; `build-changelog.py` regenerates the served changelog and needs the sweep.
- `EDITORIAL.md` A11, A14, A16, D21: locked Wolfram section names; no `src-case` chip on an external-work claim; marker forms; bridge items test the prior core.
- `SOURCES.md` header and records `src-kessler`, `src-case`, `src-pricing`, `src-google-ptcf`, `src-aa`, `src-t3-survey`, `src-magesh`, `src-dahl-fictions`: record format; `used_for.session-2` clauses to narrow; no OpenAI record exists.
- `scripts/test-editorial-regions.mjs` at HEAD: 9 passed; T7 pins session-2 at 2 literal / 64 entity em dashes.
- `session-1/index.html` 3236 to 3295 and 3362 to 3392: `COLD_CHECKS` and the reader-addressed pacing block, the source of the ritual-parity copy.
- `session-3/index.html`, `session-4/index.html` cold-open analysers: same shape as Session 2's (three regex checks; only the closing sentence differs), same markup ids; `session-0.1` has a different handler shape and is out of scope.
- `session-2/index.html` `COLE` (85 keys) and `COLEDOCS` (D1 to D10, five transcript lines): the only permitted sources of case text and figures in the fixture pack.
- `instructor-notes/session-3.md`: the note format the run sheet follows.
- No `CLAUDE.md`, no `.claude/rules/`, no `docs/changes/` exist; `MAINTAINING.md` and `EDITORIAL.md` are the governance read at Gate 0.

## Files that change
- `session-2/index.html` modified (the whole list below).
- `session-3/index.html`, `session-4/index.html` modified: the cold-open renderer (`COLD_CHECKS`), the pacing readout, and the footer heading only (DW-066, DW-074 ritual parity).
- `SOURCES.md` modified: `src-kessler` §05 to §07; `src-case` `used_for.session-2` narrowed; new `src-openai-pricing` record with `last_retrieved: [UNVERIFIED, needs source]`.
- `index.html`, `session-0.1/index.html`, `session-1/index.html`, `session-3/index.html`, `session-4/index.html` footers: regenerated by `node scripts/inject-sources.mjs` (the stamp line and, for session-2, three entries).
- `docs/unsourced-claims.md` regenerated by `node scripts/build-unsourced.mjs`.
- `docs/deferred-work.md` modified: rows DW-021, 066, 073, 074, 075, 087, 088, 089, 090, 091, 093, 094, 095, 099, 101 annotated; new rows for what this plan does not fix.
- `CHANGELOG.md` modified and `changelog/index.html` regenerated by `python3 scripts/build-changelog.py`.
- `instructor-notes/session-2.md` rewritten as the run sheet (PR D).
- `docs/changes/2026-09-12-session-2-preflight/plan.md`, `handback.md` new.
- Not touched: anything between the CASE or SOURCES sentinels, any `COLE` constant, any generated `APX*` region, `scripts/editorial-baseline.json`, `CASE.md`.

## Order of work (one edit class per step; each step is one commit)
1. This plan.
2. §00: bridge item 4 replaced per D3; bridge keys scoped and given a `.src` line (S2-12); the `retrieval-bridge` tag moved to `#bridge` (S2-46); sentences at 1235, 1242, 1246, 1251, 1272, 1273 reworded per S2-13, S2-14, S2-03, S2-04, D4, D5, DW-091; a glossary card (DW-093 amended, S2-11); tier-bar resync (S2-47).
3. Ritual parity: cold-open closing paragraph per §12.4 item 3; `COLD_CHECKS` and its renderer copied verbatim from session-1 into sessions 2, 3, 4; pacing readout and footer heading likewise (DW-066, DW-074).
4. §01: tick label and the two T = 0 strings (S2-05, DW-092 item 4); stance strings (S2-27); `.sim` and `.src` under the bars and a Wolfram chip on the 0.8 sentence (S2-05, S2-15); tag to `prediction-commit` (F-037).
5. §02: prose at 1474, 1493, 1494, 1513, 1523 (S2-21, S2-06); `.sim` to a plain span (S2-20); slopegraph chips rewired to `src-pricing` and the new OpenAI record (S2-19, S2-06, DW-020); Finding 01 chipped to `src-pricing` (DW-021); estimator retagged with a `.csrc` and honest prose (S2-22); toggle labelled by state (S2-48). `SOURCES.md` edits and `inject-sources.mjs` in the same commit.
6. §03: the Anthropic sentence and its marker cut (DW-073, §12.4 item 9, host unreachable); a mapping sentence with the `src-google-ptcf` chip (DW-021, §12.4 item 8); readout matches the Format string (S2-30); persona bars become labels (S2-33); effort-table header names the ranking; doit reworded (S2-17, DW-090); IDGT expanded at first use; tag to `builder-assembler`. `build-unsourced.mjs` in the same commit.
7. §04: `.verify` gate rewritten to the authorities the page states, §05 corrected to §07, Situation 3 and Davidson listed for the instructor (S2-07); 1603 made true for the room (DW-099); 1607 and 1643 per D4/D5; the fixture panel per §5.5 with three starter prompts labelled exercise material and the five-step procedure (DW-090); tag to `builder-assembler`.
8. §05 to §07: quiz tagged, triage retagged `t-s10c` (DW-101); 1667 claim dropped; 1689 (S2-50); buy-sell list rendered only on reveal (S2-49); doit prompt "non-voting LLC units" (S2-01); `CITES` 3, 4, 5, 6 feedback corrected (S2-09, S2-10, S2-36, S2-08); Magesh chip rewired and "over 200" (S2-35); footer stamp.
9. Appendix, §08, §09: B1 strings (S2-51, S2-39, S2-12); B2 and B3 illustrative labels and the illegal-token path (S2-40, F-048); B4 41.8% row dropped and the 3% chipped (S2-37, S2-38); B5 defence 8 to 7 minutes, lock text, the §09 card, chips at 1895, reader-addressed poll label (S2-43, DW-087, S2-38, DW-074); "section 6b" at three sites (S2-16); §08 framed async (D5, S2-32); §09 checklist item 5 and the 40% label (DW-087, S2-34); checklist retagged; deixis at 1977 and 2732 (DW-075).
10. Gate 3: the repo gate, the skill validators, the profiler and the scratch widget harness; regenerate nothing by hand.
11. Run sheet (PR D), CHANGELOG entry and regenerated page, register rows.
12. Gate 4 and Gate 5: three-pass review of the diff, `handback.md`, push.

## Golden values
| Value | Where | Derivation | Conf. | Status |
|---|---|---|---|---|
| Sonnet 5 $2/$10, Fable 5 $10/$50, Opus 5 $5/$25, Opus 4.8 $5/$25 per MTok | §02 `MODELS`, Finding 01 | platform.claude.com/docs/en/about-claude/pricing fetched 2026-09-11; introductory Sonnet 5 price now standard | H | by inspection of the fetched page |
| Sonnet 5 $2.29 vs Terra $0.52 per task = 4.4x; blended prices $4.00 vs $5.625 | §02 "fourfold" sentence | computed from the page's own `MODELS` array, (3i+o)/4 | by inspection | by inspection |
| Woelbing: Donald 30261-13 stipulated 25 Mar 2016 (no additional gift or estate tax); Marion 30260-13 stipulated 28 Mar 2016 (no additional gift tax) | §07 `CITES[3]` | McGuireWoods alert April 2016; The Tax Adviser Sept 2016 (search, 2026-09-11) | M | secondary; listed in the `.verify` gate for the instructor |
| Davidson: docket 13748-13, filed 14 Jun 2013, stipulated 6 Jul 2015, ~$2.8B asserted, ~$388M settled, self-cancelling installment notes | §07 `CITES[4]` | Stout commentary; Crain's Detroit 2015-07-08 (search, 2026-09-11) | M | secondary; listed in the gate |
| Rev. Rul. 2004-64 Situation 3: discretionary reimbursement power alone does not cause inclusion; conditioned on no understanding with the trustee; inclusion can follow from a retained removal power or creditor-access state law | §07 `CITES[2]` | model recall of 2004-27 I.R.B.; irs.gov unreachable from here | M | posed as a question in the gate; builder to confirm before 7:37 PM 09-14 |
| B5 phases 3 + 7 + 5 + 3 = 18 | B5 headings against eyebrow | arithmetic | by inspection | by inspection |
| A9 session-2 = 2 literal + 64 entity em dashes | every prose edit | `test-editorial-regions.mjs` T7 before and after | H | must reproduce exactly; any change is a halt |
| Zheng: 162 roles, 4 families, 2,410 questions | §03 (unchanged) | ACL Anthology abstract via search 2026-09-11 | H | unchanged on the page |

## Risks
- The em-dash ratchet: every reworded sentence keeps its `&mdash;` count and form; T7 is run before each commit and a delta halts the step. Riskiest step: 7 (the gate rewrite and the fixture panel carry the most new prose).
- `inject-sources.mjs` rewrites six footers; only the stamp and three session-2 entries should differ. `verify-case.mjs` and `inject-case.mjs --check` must still report six current.
- `case-inventory.mjs --report-check` must stay current: the fixture pack renders case text and figures from `COLEDOCS` and `COLE` at runtime and types none.
- Retagging exposes true adjacencies (§03 to §04 both `builder-assembler`); `validate_lesson` V6 will name it; pedagogy s3.5 says flag, not fix; recorded on DW-094.
- Nothing here is irreversible: one branch, small commits, no merge.

## Rejected options
- Trimming §00 and §09 prose for density (DW-093): the sentences carry em dashes, so a trim moves the A9 baseline, which is a halt without Jared's instruction; and it is a voice decision. Left for the manual pass.
- A "09a · Baseline · 5 min" eyebrow (§12.4 item 10): it changes the minute arithmetic and the generated budget; the run sheet moves the capture to 6:21 PM instead.
- Sourcing the Anthropic 80% claim with a new record: claude.com is egress-blocked; §12.4 item 9 authorises the cut instead.
- Propagating the renderer into `session-0.1`: its handler has a different shape and D20 puts 0.1 outside the shared architecture; flagged.
- Marking §02's "since late July" and the four-times claim `[UNVERIFIED]` instead of deleting: the clauses carry no teaching load; deletion is dash-neutral and shorter.
- A subagent fan-out for verification: the sdlc-loop skill's standing rule 2 forbids it unless asked by name; Gate 4 is single-threaded.

## Proof
- `node scripts/test-editorial-regions.mjs`: "summary: 9 passed, 0 failed" and T7 still prints "session-2 2 literal / 64 entity".
- `node scripts/verify-editorial.mjs`: "0 hard failure(s)"; A15 no longer names session-2's `src-pricing` or `src-google-ptcf`.
- `node scripts/verify-case.mjs` 6 of 6; `node scripts/inject-case.mjs --check` 6 current; `node scripts/inject-sources.mjs --check` current for all six; `node scripts/verify-migration.mjs` 15 passed with check 20 still pinning $522,086, $38,200 and 3.82%; `node scripts/build-appendix.mjs --check` current; `node scripts/build-unsourced.mjs --check` current at 11 claims; `node scripts/case-inventory.mjs --report-check` current.
- Skill validators: `validate_lesson.py` V4 PASS with 0 unresolved keys and V5 PASS; V6 still reports the count band (17) and the §03/§04 and B4/§07 adjacencies by design; `validate_dom.js` DOM mode: no script errors, all 16 gates marked (the `#pnum` line is validator drift, DW-117).
- Scratch harness (`scratchpad/harness.cjs`): every widget path OK, the tier bar lights one button, the buy-sell list shows no text before reveal, B3's illegal click does not reach -1.
- `grep -c "Session 1 baseline" session-2/index.html` returns 0; `grep -c "section 6b" session-2/index.html` returns 0.

## Departures from plan
(none at acceptance)
