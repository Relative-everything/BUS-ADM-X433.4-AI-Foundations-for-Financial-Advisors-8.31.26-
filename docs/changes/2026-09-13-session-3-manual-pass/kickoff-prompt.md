EXECUTE the Session 3 manual-pass notes on this session's branch, under the sdlc-loop and interactive-lesson-builder skills, against `session-3/index.html` on disk. First read `docs/changes/2026-09-13-session-2-manual-pass/kickoff-prompt.md` in full: it is the governing process for this pass, and everything in it applies with the substitutions below. Then read that folder's `handback.md`, `ledger.md` and `checks.mjs`: they are the worked example, and the harness is the pattern to copy. Read everything here before the first tool call.

<substitutions>
- Target: `session-3/index.html`. Change folder: `docs/changes/<today>-session-3-manual-pass/`. Ledger ids restart at JN-001 in the new folder. Notes file, ledger, plan, harness: same names, same shapes.
- The four K toggles keep the defaults in the Session 2 kickoff unless Jared's message flips one. K1 covers `session-3` (and `session-4` only where a ritual-parity ripple reaches it). K2's default stop after the ledger stands unless the message says "implement these", which pre-accepts the plan as it did on 2026-09-13.
- Batches: the notes may arrive in more than one message. Each batch appends to `notes-verbatim.md`, continues the ledger, gets its own plan section, and is executed in groups of about five items with a push after each group.
</substitutions>

<session_3_specifics>
Read these before anchoring a single bullet; each one changed what a safe edit looks like on Session 2 and will again here.
1. **The em-dash ratchet runs the other way on this page.** `scripts/editorial-baseline.json` records `session-3` as majority literal: A9 86 literal, 12 entity, 98 total; A8 minority 15. Deletions will move the literal count, not the entity count. Copy the landing helper described in the Session 2 handback (measure T7 after each edit, re-record the fallen figure, append the DW-115-style row line, require T7 green and a clean DOM-mode load, then commit) and let it re-record both forms under K1. A rise in either form is still a halt.
2. **The retrieval corpus is live data.** `CASE.md` Part O holds the ten chunks Session 3 §03 ranks and the five meeting turns; `session-3` builds an IDF index over them at load, so a one-word edit to a chunk moves every score and the margin flag. `CASE.md` is out of scope for the pass; a note that needs a chunk changed is HALTED with the ranking consequence named. Rows DW-001, DW-002, DW-003 and DW-076 carry the false "scores 0.0" verdict in three places; decision D6 lifts the VALOPT constraint before Session 3, so if the notes reach §03's verdicts, close all four together in one commit and run the four preset queries in Chromium before and after.
3. **What the room already has.** Session 2 ran on 2026-09-14 (check `audit/` for its audit and any 09-14 decisions before assuming). DW-111 records that `session-3:1247` compares the cold open against "the one you pasted in Session 1"; the series started in Session 2, so the sentence is false unless Session 2's paste happened. The Session 2 changes the instructor will expect mirrored here are in DW-117: the Shift+U override (this page binds it twice), the pacing readout's heading, paragraphs and state line (`Two ways through this session` at about line 2856), the cold open's opening line (`Not a good one. Not a work one.` at about line 1238) and closing sentence. Do not propagate any of them on your own: they are in scope only if the notes name them, and then they land the way Session 2's did (JN-001, JN-003, JN-004 in the Session 2 ledger), with `scripts/verify-browser.mjs` already scoping its override assertions to lessons that still bind it.
4. **Validators red by design on this page** are the same set as Session 2's plus its own: V2 footer hyperlinks (DW-029), V6 count and adjacencies (DW-094), `#pnum` (DW-112), and V8 plus the DOM-mode Shift+U line if the override goes (DW-116). Any other red line is a regression or a halt.
5. **No live-model console on Sessions 2 to 4** (MAINTAINING.md); a note asking for a live model here is HALTED.
6. **The spine.** DW-050 is still open and Session 2 no longer states the recurring question anywhere; if the notes cut Session 3's statement of it too, say so on DW-050 rather than deciding it.
7. **Sources.** Records added on 2026-09-13 that Session 3 can chip without a new record: `src-context-windows`, `src-api-messages`, `src-models` (re-fetched, current address), `src-pricing` (re-fetched). platform.claude.com is reachable from the build environment; anthropic.com, artificialanalysis.ai, livebench.ai, openai.com, arxiv.org, reglab.stanford.edu and irs.gov are egress-blocked, so a figure from those hosts is carried at its recorded date or marked, never re-verified by you.
</session_3_specifics>

<report_additions>
The report of the Session 2 kickoff applies. Add one line per group naming the T7 figures for `session-3` before and after, both forms, and a "Retrieval check" line whenever §03 or Part O was in scope: the four preset queries, their top-four rankings and the margin flag, before and after.
</report_additions>

Flag every constraint conflict, ambiguous note or premise error explicitly rather than resolving it silently. A reasonable guess is not a resolution.
