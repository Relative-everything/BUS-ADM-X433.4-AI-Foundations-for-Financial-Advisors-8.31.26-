# Session 2 manual-pass notes, verbatim (batch 1, 2026-09-13, from chat)
Transcribed as written, one bullet per line; typos kept. Redactions under kickoff K3: none needed (no learner name, no platform name, no retired string).

I am going to do this concurrently as i write the updated, so first implement these changes - it is largely removing useless filler text taht does not add to teh session: Overall updates:
* Remove the "shift + U" to reveal - there is nothing hidden anymore
* Remove ALL of this worthless/chaff text:
   * "The Cole household
   * Synthetic case — teaching anchor only
   * One fabricated household carries every client example. It is not your final-project dataset: Part 1 runs on a real recurring task from your own practice, with your own synthetic or de-identified data.
   * Household
   * Meg and David Cole, Barrington Hills, Illinois
   * Meg runs Cole Precision Components Inc.; David is a retired hospital pharmacist. US citizens, Illinois-domiciled for life, no liabilities. Balance sheet in the case panel.
   * The asset
   * CPC stock: one asset, most of the estate
   * A Rockford C-corporation paying its sole shareholder a large dividend every year. Meg bought every share from her father, Walter Hensley, so her basis is what she paid. A competitor has made an unsolicited written inquiry; no letter of intent is signed, and Nathan Cole, in CPC operations, has not been told.
   * The structure
   * Non-voting LLC units to be moved to a grantor trust
   * A seed gift of non-voting units, then a sale of a much larger block to the same trust for an interest-only demand note, at a discount with no appraisal and no appraiser engaged. Illinois imposes its own estate tax at a threshold neither indexed nor portable; neither spouse has engaged with it.
   * The question every session works on
   * How much of the note does she call this year?
   * Meg is short $522,086 a year from year 6 while the structure performs exactly as designed. Each $1,000,000 she calls permanently removes $38,200 of future interest, so the gap widens by 3.82% of every call. What does calling it cost her in every year after, and when is a different lever the better answer? No session answers it.
   * Every figure in these cards is the case file's; the household is constructed L.
   * Outcome 01 / A working model of sampling / Why the same prompt returns different text, and what that does to an audit trail.
   * Outcome 02 / A tier-selection rule / Cost per finished task, benchmarked, rather than cost per token.
   * Outcome 03 / Your own prompts, scored and rewritten / Three templates, yours or the workshop's, scored element by element; the weakest rebuilt in place.
   * Outcome 04 / A citation verification routine / Four checks, in order, applied to tax authority you did not write.
   * Nine terms this session uses before it defines them / Token: the unit a model reads and writes, roughly three quarters of a word; prices and context limits are counted in it. Temperature: the setting that decides how often the model picks a lower-ranked next word instead of the top one. Persona, Task, Context, Format: the four parts of a prompt, who the model writes as, what it does, what it may use, and what shape comes back. Template: a prompt written so that a second person can run it on a new input without asking you anything. Grounded: an answer built only from a named source the model was given to read, so each claim can be checked against that source. Tier: a model size and price class; the choice is made on cost per finished task, not cost per token. Baseline: how long the task takes you today, unassisted, recorded before any tool touches it. IDGT, intentionally defective grantor trust: a trust drafted so that the grantor stays liable for its income tax while the trust, not the grantor, is intended to own what it holds. Non-voting units and a demand note: the LLC interests being sold carry economics but no vote, and the buyer's note can be called for its principal at any time.
   * Before we start / Open the AI tool you subscribed to, in a new tab, now. Every section below has something to run.
   * Open the three prompt templates you wrote — or use the three supplied in section 04, the workshop.
   * · Work along / Answer all four bridge items and commit each before revealing anything."
* Change "the last prompt you sent" box - it should JUST say: the last prompt you sent> (box for input)>analyze what ZI typed. Remove all of this text:
   * First run. Keep the result on screen; Session 3 runs the same field and the drift between the two is the measurement.
   * · Work along / Paste your last prompt and run the analysis. Keep it on screen — the comparison across sessions is the point.
* APPENDIX BOX :
   * On the appendix box that starts with "how this session is paced" - remove all the text excet the interactive buttons of foundations/standard/advanced/core only and the estaimted timing per the 11 core/5 appendix/everything. So this how the session is paced box should cut ALL of this text:
      * "Two ways through this session. The core is 11 sections and about 69 minutes. It stands on its own: nothing in the core depends on the appendix, so reading those sections in order gives you the whole argument.The 5 appendix sections add 81 minutes of optional depth, 150 minutes for everything. Each one is placed in the core at the point it belongs rather than at the back of the file, so you can carry straight on into it and then pick up where you left off. The gold cards under Appendix contents link to each one, and the depth control above sets how many are showing; anything hidden leaves a stub naming what it was, so you can always see what you skipped. They also read on their own afterwards. Core only — appendix hidden. 69 core minutes"
* Retrieval bridge:
   * Either completely remove OR Totally redo this and replace all the questions, only have 3 questions - focus on the topics I covered specifically in lesson 1 per hte transcript
      * 1 question on tokens - either what they cost or just what are they
      * 1 question on context - what gets sent along with the text int he context window of a prompt
      * 1 question on when to start a new chat - make sure this actually has evidence to support why/when to start a new chat

# Batch 2 (2026-09-13, chat), verbatim; one redaction under K3: the submission platform's name, twice, in the §08 bullet
Here are remainnig updates to implement: 1 - temperature and output variance:
* The section Commit before the content runs is good BUT needs these changes:
   * After you select the options, it should list out all the possible differences between running the same prompt in teh same model an hour apart:
      * Any other memory changes on the account
      * The hardware the prompt is run on itself change
      * Any other context provided
      * Etc
         * Really just list all the possible things that could influence the same prompt/model evne just 60 min apart so students have clear examples after they click on their choice of the 2 optinos
   * Try to reduce this text by at Lest 50% ideally 75%, simplify - this is currently too much jargon or too advanced for the average student in the class, the current tier of complexity is at least 100% if not 2-300% higher than where students are now based on their feedback:
      * TEXT TO REDUCE "If the model always picked the top-ranked candidate, Wolfram notes, you get flat text that sometimes repeats word for word. Picking lower-ranked candidates sometimes produces more interesting text. The parameter governing how often that happens is called temperature, and Wolfram is explicit that there is no theory behind the value — 0.8 is simply what has been found to work for essays. H / The slider below applies the actual transformation: each candidate's probability is raised to the power 1/T and the set is renormalised. Low T sharpens the distribution toward the leader. High T flattens it. Then the panel samples the same prompt five independent times so you can see the spread."
   * Rewrite this section, I like the 3 cards, but totally remove the comparison 4 minutes - this is way too much text and a bad activity idea since students wont undestnad how to do it:
      * Totally delete this text: "Comparison — 4 minutes / Send this to your own tool three times, changing nothing between runs: Write one sentence on the best thing about AI. How different are the three? Ask one person to read all three of theirs aloud. / Then run this, which constrains the output space rather than the sampler: Answer in exactly one sentence, in the form "The best thing about AI is its ability to X." Choose X from: reason, retrieve, summarise, generate. Output nothing else. Run it three times. Note how much of the variance disappeared and where it went. / · Work along / Run the same prompt three times at one temperature, then constrain the output space and run it three more."
      * Then update this text, change consequence 1 to be more FINRA/SEC compliance specific (ie. Having a prompt is not an audit trail and would be flagged in the annual compliance review), 2 is great, change consequence 3 since that is just a fact not a consequence, also try ot shorten all 3 of them: "Consequence 01 / The prompt is not the record / At any temperature above zero the same prompt produces different text. An audit trail that stores only the prompt has stored the input to a random process, not the output you relied on. / Consequence 02 / Re-asking is not verification / Running the query again draws a second sample from the same distribution. Agreement between two samples is evidence about the distribution's shape, not about the tax code. / Consequence 03 / Consumer chat tools do not expose T / You cannot set temperature in most subscription interfaces. You control variance through the prompt — by constraining the output space — not through a setting"
* Section 2: economics
   * Try to reduce wordcount in thai section without losing meaning -= the first 2 paragraphs especially should be reduced. The content is good, but this is meant as a visual aid not a book the students read themselves.
   * REMOVE all references to "verification needed" - do not add disclaimers here, that is what the bibliography file in the repo is for.
   * Most important thing - ADD IN CLEAR EXAMPELS OF EXACTLY WHAT A "completed tasK" actually is defined as, ideally if you could use examples that people/stuendts in wealth management would understand that is best. We cannot keep talking about cost per task without clearly defining what a task is in ai comparison.
   * Remove all this extra text: "Decision — 4 minutes / Name the tier you will use for your final project workflow and the reason in one sentence. Acceptable reasons: measured cost per task, a capability the lower tier failed at, a context-window requirement. Not acceptable: it is the newest, it is the most expensive, it is the default. / Verification required before this slide is relied on / Model tiers and their subscription availability change between cohorts. Confirm which tiers your own plan currently reaches at claude.com/pricing before you build a workflow that depends on one. Benchmark rankings are recomputed continuously and can reverse between one pull and the next. / · Work along / Estimate your own monthly cost, then find the tier where cost per finished task beats cost per token"
   * Reverify all the numbers in this section in terms of token pricing/outputs against livebench and other updated sources - ensure that all models available as of 9/13/26 are updated
   * Update the "Your own monthly cost estimate": -- it is not good interactgivitgy because it just basically changes the number. Make some new version of interactively showing teh cost per month, ideally ussing a blend of models. Maybe the slider bars could be in terms of "complex to simple" or repeatable vs undefined? Make the interactive more custom to the person and make it more approachable, and ensure whenever students interact wit the page it should be unique and changing - not just showing different numbers over a static graph.
* 3 - framework (persona task context format)
   * This section is very good, but reduce word count by 20-50% - ideally reduce the first 3 paragraphs most, Keep almost all of "what the evidence says about persona" - that is a greate teaching opportunity
   * Remove all of this text: "Rebuild of the prompt — 5 minutes / Take the weakest of the three templates you brought, or the weakest of the three supplied in section 04. Identify which of P, T, C, F it is missing. Add the missing components and run it. Keep both versions — you will need the before-and-after for the peer review before Session 3. / · Work along / Assemble one prompt with all four P.T.C.F elements and read the assembled text back"
* 4- workshop:
   * REMOVE all instructor checks/disclamers like this "Requires instructor verification before it is taught as settled"
   * Remove all this text: "When you wrote yours, you had heard eight checks read aloud and had not seen them rendered, scored or named as a framework. That gap was deliberate. / The sequencing exists so that this comparison is available to you. A framework taught before an attempt is a set of rules you comply with. A framework taught after an attempt is a diagnosis of something you already produced, and the gap between the two versions is yours rather than mine. Wrong first attempts followed by correction outperform being told the right answer first, which is the same finding that governs every prequestion in this file. / Requires instructor verification before it is taught as settled / [the six-item verification list, ending] Kessler v. Commissioner is a deliberately fabricated citation used as exercise material in §07, labelled on the page and in the footer"
   * Ideally redo this interactivity in this section - instead of just providing 3 prompts, make it so the 3 prompts can be clicked on and you can highlight the section of each of the 3 example prompts that are PCTF
   * In the section with "Prompt 1 / Prompt 2 / Prompt 3 / Scoring prompt 1 / Paste the prompt exactly as you wrote it, typos included": Make it so that instead of you ranking the prompts, it shows the 3 prompts that were premade from above -- as you click on absent>gestered at > specified>specificeda nd checkable it will change the prompts themselves. So basically i want this to be like the students can click on each level of absent >sp[ecific/checkable for each of PTCF and see how the prompt is build one step at a time, and what gesteured at for a format may look like vs specific and checkable for format,.
   * Basically update the whole interactivity of the "scoring" section to be building prompts step by step using the 3 premade example prompts, but making versions of the prompts with 4 different levels of PTCF so the students cna clearly see versions of bad/absent and then click through to see the changes that get them to specific/checkable
   * The interactive "rewrite hte weakerst" is great- especially preloading teh scafflord is very good.
* 5 -diagnostic
   * Delete this text: "Discussion — 3 minutes / Item 7 is the one built to split the room. Argue it out before you look at the feedback."
   * Other than that. This one is good -- perhaps rethink questions but overall no big changes needed here
* 6 - technique: the interview rewrite
   * FIX this interactive tick each question -= -when i click on each one it just highlights green and wont reveal until i clujck the bottom button that says reveal the questions - fix this so it reveals them one by one as i click, not all at once from the bottom button .
   * Consider either replacing this section or adding 1 more interactive item: Create a reverse prompting interactive section where you can click on different creative question ideas to start a reverse prompting interview where the model helps you write your own prophet. Perhaps have a few short prompts that kick off this questioning ("my child is having a birthday in 2 weeks, help me plan the party starting with questioning me to learn about their interests and atenedees" and others like that)
   * You can keep the name, but in parentheses add "reverse prompting" since that is the name of modelinterview to create a prompt
   * The example prompt is good, but again this is way too much text for a visual aid, reduce by at least 50% of text, ideally remove the restatement of the cole synthetic case facts: "The Cole Buy-Sell Agreement / Synthetic case The buy-sell agreement CPC inherited was executed in 2014 and never updated. Its formula produces a value far below the range the company is now indicated at, and the file holds three different values for the same company, each from a different date, a different preparer and a different purpose. You want a summary of what it does and where it breaks"
   * Remove this text: "Standing constraint / No real client data and no personal identifying information enters any AI tool at any point in this course, including the box above and including your final project transcript. Synthetic or fully de-identified facts only"
* 7 - verification
   * Give a direct source for this claim or remove it: "Purpose-built retrieval-grounded research products from LexisNexis and Thomson Reuters — sold on the claim that retrieval eliminates the problem — hallucinated on between 17 and 33 percent."
   * Fix the html since this text is displayed outside the box, it is running over the margin: "VENDOR MARKETING AT THE TIME CLAIMED "100% HALLUCINATION-FREE LINKED CITATIONS." THE STUDY FOUND THE CLAIM OVERSTATED"
   * This section is way too advanced, but it is needed the "Citation triage — classify each". I will plan to just use 2-3 of these to show students how i actually verify ai citations - but going find the exact sentence that the citation states and then ensuring it is relevant. I would plan to follow the 4 failure typeds process to teach them - though clarifying how to check if tax law is superseded would be useful, I may use that as an opportunity to teach that tax law is constantly changing and based on court cases so no 100% perfect recrod exists.
* 8 - workshipo template audit
   * Remove this - remember the repo stands alone and is indepdent of [the submission platform], so do not refer to [the submission platform] assignemtnms: "This part runs between sessions: the instructor pairs you after this session, you swap templates, and you run their template on the Cole facts without asking them anything, before Session 3. It is the Session 5 exercise in miniature and the fastest way to discover that a template you find obvious is unusable by anyone else."
   * The entire "stuctured peer review" section should be redone: Instead of peer review it will be me the instructor running a live audit on a prompt in class - i will have one prepared but will ask the students if they want ot provide me one to review. We are NOT going to run this in class, so remove the text that revers to sharing prompts with classmates during session 2 class
* 9 assignment --
   * Shorten this significantly: You can keep what it is/handoff but remove the two absolute rules. Keep part 1 deliverable checklist. Delete all of the first draft and all text below: "The First Draft / A first draft of Part 1: the target task, your model and tier choice with the reason, and your initial prompts. §02 is where the tier reason comes from. This section is where the target task comes from. §04 produced at least one of the initial prompts. You have all three already. / Reading for Session 3 / Wolfram — "The Concept of Embeddings" and the opening of "Meaning Space." Kitces (January 2025) on AI notetaker adoption and satisfaction. Iskowitz (July 2025) on notetakers and compliance. / What Session 3 does that this session could not / [two paragraphs on grounding and the corpus] / · Work along / Record your baseline before you leave. It is the one thing in this session you cannot reconstruct later. / Closing question · one to three answers, out loud, then we go / Your rewritten template just produced a clean, well-sourced answer on the Cole discount. Name the one check you would still run before any of it reaches Meg, and say why the answer looking right is not a reason to skip it."
* Appendix B 2 - laplace :
   * Keep the 2 objective side by side, but then add a new interactiv element in this section as described below here: Task: Add one interactive element, "Three Cups," to the Laplace section of session-2/index.html, implementing Block 0 Option A of the attached draft as a click-through instead of live props. Include one short explanation paragraph tying it to the Session 2 AI principle. Behaviour (4 clicks, in this order): 1. PREDICT. Before Start, one question with four buttons: "After you lift two empty cups, what is the chance the coin is under the last one?" Options: 33%, 50%, 66%, 100%. Selection locks. (Correct: 100%. 66% is the additive error; 33% is "nothing changed"; 50% is "stopped after one lift.") 2. START. Three identical closed cups appear, labelled A, B, C. Readout above them: "Chance the coin is under any one covered cup: 33%". 3. LIFT ONE. Student clicks any cup. It shows empty. Readout updates to 50%. Lifted cup becomes non-interactive. 4. LIFT ANOTHER. Student clicks either remaining cup. It shows empty. Readout updates to 100%. The last cup stays covered, visibly highlighted, with the caption "The coin is here. You never lifted it, and the number still reached 100%." A "Show coin" control is optional; if present it is the 5th click and reveals the coin under the last cup. 5. GAP REVEAL. Show the student's prediction from step 1 beside the actual final value (100%), rendered as a visible gap, not text only (pedagogy s3.3). If they chose 66% or 33%, one line: "The number moved because what you knew moved. Nothing about the last cup changed." 6. DISCLOSURE, shown after step 4 on the page: "This demo always places the coin under the cup you do not lift, so everyone reaches the same beat. The chances shown are the correct ones for what you knew at each click." (Constructed artefact, labelled on page.) 7. RESET. A "Play again" control returns to step 1 with the prediction cleared. Explanation paragraph, placed directly beneath the element, verbatim: "The coin never moved. Your number moved three times, and each time it moved because what you knew changed. An AI chatbot works the same way, one layer down: it writes each word by guessing what comes next, based on what it has read and what you typed. When it sounds certain, that certainty is your number, not the coin. It describes what the model has seen; it does not describe what is under the cup, in the tax code, or on your client's return. Only lifting a cup changes the number. Only opening the source changes the answer." Attach one .conf chip [H] to the sentence "it writes each word by guessing what comes next" with data-src resolving to the Wolfram (2023) footer entry, assigned section "It's Just Adding One Word at a Time". Do not quote Wolfram. Constraints (ranked, MUST > SHOULD > PREFERRED): 1. MUST: readout values are exactly 33%, 50%, 100%. Never 66%. 2. MUST: plain ES5 IIFE, no framework, no build step, no external dependency, no browser storage (pedagogy R8, R9, R12). State lives in JS variables. 3. MUST: keyboard-reachable. Cups and buttons are <button> elements, not divs with click handlers. Visible focus states. Lifted/covered/highlighted states are conveyed by text and shape, not colour alone (s7). 4. MUST: root carries data-task="t-<next unused n>" and data-comp="<family slug for prediction + gap reveal>". Every .conf chip has a data-src resolving to a footer <li id="src-...">. 5. MUST: harvest from component-library.md where a matching family exists; anything written new is marked [NEW] (R11). 6. MUST: no section gating. The element does not lock, hide, or block anything after it (R10). 7. SHOULD: the prediction step (step 1) is retained. It is what lifts the element from Active to Constructive (s3.1). If the user later asks to drop it, the element still counts as an interaction but no longer as a Constructive one; say so in the report. 8. SHOULD: the three cups fit at 380px width without horizontal scroll. 9. PREFERRED: cup lift is a simple CSS transform, no animation library, no images. A cup drawn in inline SVG or CSS shapes is fine. Timing: this element replaces the live-props version of Option A inside the same 3-minute opener budget. Do not change the section's minute allocation. If the Laplace section does not yet exist in the timing table, stop and report; do not absorb 15 minutes silently. Must pass before commit: validate_lesson.py, validate_dom.js, restyle_sweep.py --check. All exit 0. Then a Playwright screenshot of the element in all four states (predict, 33%, 50%, 100% with gap reveal) at 380px and 1024px. Out of scope: every other section of session-2/index.html; the timing table except as noted; assets/*.css; the footer except the one Wolfram entry if it does not already exist; any other session file. Report back: (1) the data-task id and data-comp slug used; (2) whether the element was harvested or [NEW], and from which library entry; (3) new interaction count for the session and distinct component types, with any consecutive same-type repeat named; (4) validator commands with output; (5) screenshot paths; (6) any timing-table change; (7) anything in the draft you could not implement as specified.
