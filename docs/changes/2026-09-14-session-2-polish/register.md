# Register: Session 2 polish pass, 2026-09-14
One row per finding, ids in page order. Anchors are stated against HEAD 88d89c9 (the merge of PR #27). Column meanings and the FIX test: `kickoff-polish.md` in this folder, `<intake>` and `<classes>`. Status is filled during execution; a FIX row without a Status has not been taken yet, and a resumed session starts at the first such row.

Survey coverage, all seven dimensions across all 17 sections (`s0`, `apx`, `s1`, `s2`, `sCold`, `s3`, `s4`, `s5`, `s6`, `s6b`, `s7`, `s8`, `s9`, `s10`, `s11`, `s12d`, `s12`), read in full from disk at 88d89c9 before any row was written:

- D1 seams: every connective, pointer and repeated sentence checked against `grep -c` of its target. Found in `s1`, `s3`, `s8`, `s9`, `s10`, `s11`. Nothing in `s0`, `s2`, `sCold`, `s4`, `s5` (the intro paragraph's separation from its chart is the instructed order, see PL-010), `s6`, `s6b`, `s7`, `s12d`, `s12`.
- D2 sentence craft: every `p.big`, lede, card set and list read for one idea per sentence and parallel shape. Found in `s3`, `s5`, `s12d`. Card sets in `s5` (tasks, kinds, findings), `s7`, `s10` (checks) and `s12` are parallel; `s3` and `s10` (types) are asymmetric by instruction or by taxonomy (PL-007).
- D3 terminology: `grep -c` over task, tier, model, prompt, template, context window, chat, and the section-reference forms. Task and benchmark task are distinguished by the two `s5` headings; tier (13) means a price tier and model means a named model throughout; template is defined against prompt in `s11`; window and box are used as the bridge defines them. One competing form found: "section 04, the workshop" in `s11` and `s12d` against `§0N` everywhere else (PL-028, PL-032). Deliberate substitutes recorded, not normalised (PL-014).
- D4 headings, labels, controls: every `h2` and `h3` against B7, every button against its click, every `label` against its control, every eyebrow against the run sheet and `build-appendix --check`. Found in `s6`, `s8`; minutes recorded as LEFT-AS-IS (PL-001, PL-037).
- D5 interaction quality: all 19 roots clicked in Chromium (`scratch probe.mjs`, 2026-09-14): zero page errors, every control a button, input or textarea, every readout below its control. Found: unnamed groups and colour-only state in `s6b` and `s11`, unlabelled inputs in `s12d`, an invisible focus ring on the `.tg` toggles in `s2` and `s6`.
- D6 uniqueness: the five `work-along-gate` roots and the four `builder-assembler` roots compared member by member (the table is in `recommendations.md` and the report). Found: two mis-tagged families in each set (PL-003), one true pair (PL-017), one same-mechanism pair kept (PL-031).
- D7 mechanical: every id in script against the markup, every `data-src` against the footer (V4: 54 resolve), every lesson-CSS selector against the markup, every figure against its other statement on the page and in `SOURCES.md`, every run-sheet element id against the page. Found in `s5`, `s7`, `s10`, the footer, the page CSS and the committed profile.

Before figures (HEAD 88d89c9, measured this session): `node scripts/test-editorial-regions.mjs` T7 session-2 2 literal / 38 entity, 9 passed; profiler 17 sections (16 lesson, 11 core), timing 150/150, 19 roots, ICAP P 0 / A 6 / C 12 / I 1, consecutive pairs `s1->s2`, `s6->s6b`, `s9->s10`, prose words per minute median 27.3; `checks.mjs` (09-13) 19 OK; 400 px `scrollWidth` 400 at core-only and at +Standard (no overflow at 400; DW-119's two pixels are at 380).

| PL | Dimension | Section · anchor at 88d89c9 | What reads now (quoted) | What it becomes, or why it stays | Class | Conf | Pri | Cross-ref | Test | Commit | Result | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| PL-001 | D4 | `#s0` · the eyebrow · L1229 | "6 min" on the section that is now the bridge and the depth control | Stays. The bridge is ratified at seven minutes and the run sheet gives it seven at 6:14 PM; the six is the section's figure and moving it moves the 150 arithmetic and the generated budget, which DW-105 already holds open for the instructor. | LEFT-AS-IS | H | P3 | DW-105 | `build-appendix --check` current at 6; run sheet s0 slot reads 7 min | (none) | examined, kept | LEFT-AS-IS |
| PL-002 | D1 | `#s1` · the live-run doit, last sentence · L1304 | "&sect;01 runs the same comparison with the output space constrained." | The target is gone: the §01 doit "Comparison, 4 minutes" (send one sentence three times, then constrain the output space) was deleted by JN-008 in 34ecc55; this sentence entered on 09-12 (9237067) to point at it. Reads "&sect;01 explains the difference and how to narrow it." Nine words for ten. | FIX | H | P2 | JN-008; 09-12 commit 9237067 | GREP "runs the same comparison" = 1 then 0; GREP "Comparison &mdash; 4 minutes" = 0 at HEAD (the target); DOM checks.mjs PL-002 | d9e3d2c | grep -c "runs the same comparison" 1 then 0, "Comparison &mdash; 4 minutes" 0 (the target, at HEAD and after); checks.mjs PL-002 OK; T7 unchanged 2 / 38 | LANDED |
| PL-003 | D6 | `#s1` L1278 (`t-s1`), `#s4` L1446 (`t-s4`), `#s8` L1691 (`t-s8`), L1699 (`t-s8b`) | `t-s1` and `t-s4` carry `data-comp="work-along-gate"`; `t-s8` and `t-s8b` carry `builder-assembler` | Four roots are tagged as a family whose contract they do not meet. `t-s1` (click a ranked bar, the next distribution recomputes) and `t-s4` (the paren predictor, named as an anchor of family 7) are `distribution-picker`; `t-s8` reveals ten fixed questions one click at a time and assembles nothing (`spoiler-reveal`, family 2); `t-s8b` swaps a prewritten kickoff and question list per starter (`symptom-diagnoser`, family 14, or `spoiler-reveal`). Retagging moves the ICAP spread, V6's adjacency list and the run sheet's `data-comp` labels, and reverses part of the 09-12 "honest retagging", so it is the instructor's call. | PROPOSE | M | P2 | DW-094, DW-101, S2-28, F-7 | the per-member table in recommendations.md; profiler `consecutive_family_repeats` before and after | (none) | reported, not landed | PROPOSED |
| PL-004 | D5 | page CSS `.tg` L554-561; instances `#s2` L1322-1323, `#s6` L1556-1559 | `.tg input{position:absolute;opacity:0;width:0;height:0}` and no rule on the label | A keyboard user who tabs to a toggle sees nothing: the focused checkbox is 0 by 0 at opacity 0 (probe: `active: cbP`, `inputBox [0,0]`, `labelOutline none`). One rule after the fence closes it: `.tg input:focus-visible+.track{outline:2.5px solid var(--on);outline-offset:3px}`. It is page-level CSS reaching two sections, so it fails FIX line 1. | PROPOSE | H | P2 | none | probe.mjs focus read; the same read after the rule | (none) | reported, not landed | PROPOSED |
| PL-005 | D2 | `#s3` · the `p.big` · L1411 | "Wolfram notes there is no theory behind that number, it is simply what works for essays." | A comma splice, authored by the pass on 09-13 (77c6300, JN-007), not by the instructor. The comma becomes a colon: "no theory behind that number: it is simply what works for essays." Same words, same chip. | FIX | H | P1 | JN-007 | GREP "that number, it is" = 1 then 0, "that number: it is" = 0 then 1; DOM checks.mjs PL-005 | c5e782d | grep -c "that number, it is" 1 then 0, "that number: it is" 0 then 1; T7 9 passed, session-2 2 literal / 38 entity; checks.mjs PL-005 OK | LANDED |
| PL-006 | D1 | `#s3` · the lock message for the second position · script L2907 | "the third consequence card is the part most people have not thought through" | Stays. The pointer resolves: the third card is now "Control variance with the prompt", and that you cannot set temperature in the chat product or on current models is still the part the room has not thought through. The 09-13 handback listed it as a nit; a rewrite would be taste. | LEFT-AS-IS | M | P1 | JN-009; 09-13 handback nit 1 | GREP "third consequence card" = 1; the card's h4 at L1433 | (none) | examined, kept | LEFT-AS-IS |
| PL-007 | D2 | `#s3` · the three consequence cards' h4 · L1431-1433 | "A prompt is not an audit trail" / "Re-asking is not verification" / "Control variance with the prompt" | Stays. Two declaratives and an imperative, because the note for JN-009 said "change consequence 3 since that is just a fact not a consequence": the third card is the action the other two lead to, on instruction. | LEFT-AS-IS | H | P1 | JN-009; notes-verbatim L56 | GREP each h4 = 1 | (none) | examined, kept | LEFT-AS-IS |
| PL-008 | D3 | `#s3` · the `p.big` L1411 against the list item L1399 and the tick L1418 | "At 0 it takes the top word every time" against "even a setting of 0 is not fully deterministic" and "greedy: top bar every time; real APIs still vary" | Stays. The sentence states the mechanism as Wolfram gives it, at the level JN-007 asked for; the slider tick and the first list item carry the vendor's caveat in the same section, and the run sheet delivers DW-092 item 4 at the slider. Adding "in this simulation" would raise the count in a paragraph the note cut by two thirds. | LEFT-AS-IS | M | P1 | JN-007; DW-092 item 4 | GREP "real APIs still vary" = 1, "not fully deterministic" = 1 | (none) | examined, kept | LEFT-AS-IS |
| PL-009 | D2 | `#s5` · the `p.big` · L1477 | "Two models priced within two dollars of each other per token can differ fourfold in what a finished piece of work costs" | "per token" read literally is wrong by six orders of magnitude; the lede's first sentence names the unit. Pass-authored on 09-13 (b3cb9e8, JN-010). Reads "Two models two dollars apart per million tokens can differ fourfold in what a finished piece of work costs". Eight words for ten. Derivation of "fourfold" on the page's own data: Sonnet 5 blends to $4.00 and Terra to $5.63 at the slopegraph's 3:1 ratio, $1.63 apart; their per-task costs are $2.29 and $0.52, a ratio of 4.4 (M, derived from `MODELS`). | FIX | H | P1 | JN-010; DW-118 | GREP "per token can differ" = 1 then 0, "two dollars apart per million tokens" = 0 then 1; DOM checks.mjs PL-009 | 67ffe91 | grep -c "per token can differ" 1 then 0, "two dollars apart per million tokens" 0 then 1; T7 unchanged 2 / 38; checks.mjs PL-009 OK | LANDED |
| PL-010 | D2 | `#s5` · the second paragraph · L1479 | "Plotted together, the price-sheet ranking falls apart." | A dangling participle: the ranking is not what is plotted. Pass-authored on 09-13 (b3cb9e8, JN-010). Reads "Plotted together, they scramble the price-sheet ranking." Seven words for seven; "they" is the score and the cost in the sentence before. The paragraph now sits two blocks above the chart it introduces (JN-012 and JN-037 inserted "What a task is" and "What a benchmark task is" between them); that order is instructed and stands, and the benchmark block's closing sentence re-introduces the chart. | FIX | H | P1 | JN-010, JN-012, JN-037 | GREP "the price-sheet ranking falls apart" = 1 then 0, "they scramble the price-sheet ranking" = 0 then 1; DOM checks.mjs PL-010 | fbd35d7 | grep -c "the price-sheet ranking falls apart" 1 then 0, "they scramble the price-sheet ranking" 0 then 1; T7 unchanged 2 / 38; checks.mjs PL-010 OK | LANDED |
| PL-011 | D7 | `#s5` · the frontier chart's source line L1512 against the benchmark paragraph L1491 and the footer L1999 | "Index scores and per-task costs for Opus 5, Fable 5, Sol, Opus 4.8 and Sonnet 5 <H src-aa>" | The same record is chipped H on the chart and M on the paragraph above it, and the footer entry and `SOURCES.md` carry M (`confidence: M`, retrieval "2026-08", three pulls that disagree, DW-118 lists the figures for a browser re-check). The chart's chip should read M to match its record. A chip change is PROPOSE by class. | PROPOSE | H | P1 | DW-118; src-aa; F-1 | GREP `data-src="src-aa">H` = 1 now; V4 after | (none) | reported, not landed | PROPOSED |
| PL-012 | D7 | `#s5` · Finding 02 and Finding 03 · L1518-1519 | "$2.29 against $2.75" and "26%" with no chip on the card | Stays. Both are arithmetic on the two chipped figures in the chart's source line (2.75 less 2.29 over 2.75 is 16.7%; 2.75 less 2.03 over 2.75 is 26.2%); a chip on the card would restate the same key, and the 09-12 pass narrowed these cards to what the chart shows. DW-118 lists both for the browser re-check. | LEFT-AS-IS | H | P1 | DW-118; 09-12 handback | arithmetic shown here; GREP "$2.29 against $2.75" = 1 | (none) | examined, kept | LEFT-AS-IS |
| PL-013 | D4 | `#s5` · the whole section · L1474-1543 | 1,077 words for a 6-minute eyebrow after JN-012 and JN-037 (profiler at HEAD: 179 words per minute, prose 161, against a page median of 27.3) | The two blocks were asked for and the run sheet reads the benchmark block rather than running it, so nothing is broken tonight. For the async reader the six kind cards are 340 words in a section priced at six minutes. Options for the instructor: fold the six cards behind a "Show the six kinds" control; cut each card to its prompt and one scoring clause; or move the block to a new appendix section after §02 (which changes the count and the budget). | PROPOSE | M | P2 | DW-093, JN-012, JN-037, F-7 (batch 3) | profiler `words` and `words_per_min` for s5, before and after | (none) | reported, not landed | PROPOSED |
| PL-014 | D3 | `#s5` · L1479, L1512 against L1491, L1500-1501 | "Artificial Analysis" (3) against "the testers" (2) | Stays. The plain-language substitute is JN-037's register choice for a block written for a room the notes call two to three times too advanced; the name appears two paragraphs above and in the chart's source line, and the M chip on the paragraph resolves to the same record. | LEFT-AS-IS | H | P1 | JN-037 | GREP "Artificial Analysis" = 3, "the testers" = 2 | (none) | examined, kept | LEFT-AS-IS |
| PL-015 | D7 | `docs/audits/profiles/session-2.json` L791-792 against the page | The committed profile records `s5` at 610 words / 499 prose; the profiler at HEAD measures 1,077 / 966 | The profile was regenerated after batch 2 and not after JN-037 (batch 3), so the kickoff's "before figure" is stale for one section. Every other section and every summary figure agree. Regenerated by its generator in the records commit; the before figures this pass reports are the HEAD measurement kept in the scratchpad. | FIX | H | P3 | none | `node scripts/audit/section_profile.mjs session-2/index.html`, then the per-section diff against the previous file | ceea84f | node scripts/audit/section_profile.mjs session-2/index.html (jsdom): s5 610 / 499 in the committed file, 1,077 / 966 at 88d89c9, 1,074 / 963 after the pass; every other section equal or lower; 19 roots; ICAP and pairs unchanged | LANDED |
| PL-016 | D4 | `#s6` · the panel label · L1592 | "A related result on prompt length" above "Longer prompts are not better prompts. Specific prompts are better prompts, and specificity is usually shorter than the padding it replaces." | The label promises a study result; the panel carries no source and no chip, and the sentence is the course's own rule in the register of "Price is never the reason to use the cheap tier". The label becomes "A related point on prompt length". One word; the claim is unchanged. Present since 08-22; no audit row names it. | FIX | H | P1 | none | GREP "A related result" = 1 then 0, "A related point" = 0 then 1; DOM checks.mjs PL-016 | cdc200a | grep -c "A related result" 1 then 0, "A related point" 0 then 1; T7 unchanged 2 / 38; checks.mjs PL-016 OK | LANDED |
| PL-017 | D6 | `#s6` L1554-1566 (`t-s6`) and `#s6b` L1605-1625 (`t-s6b`), consecutive in the core order | Both ask the learner to set the four P.T.C.F elements and watch a prompt assemble: `t-s6` toggles each on or off and adds a simulated "what comes back" and a verifiability score; `t-s6b` sets a level from absent to specified and checkable on three premade prompts and loads the result into the editor | The second is the richer version of the first act, and the learner performs it twice in a row. Option A, about 30 minutes: `t-s6` keeps its toggles, the "What comes back" panel and the score, and drops its "Assembled prompt" panel, so §03 shows what an absent element does to the output and §04 builds the prompt; count holds at 19, the adjacency stands. Option B, about 60 minutes: the "What comes back" panel joins the §04 builder and `t-s6` goes, count 19 to 18, but §03 then has no interaction and needs one. Recommendation A. | PROPOSE | M | P2 | DW-094, DW-101, F-7; JN-016, JN-020 | the comparison table; profiler families and ICAP before and after | (none) | reported, not landed | PROPOSED |
| PL-018 | D5 | `#s6b` · `tiles()` · script L2966 | `<div class="scale" data-e="P">` with four `aria-pressed` buttons and no group name (probe: `role null`, `label null`) | A screen reader hears "Absent, Gestured at, Specified, Specified and checkable" four times with nothing saying which element each row sets. The scale gains `role="group"` and `aria-label="<element> level"` in the one function that renders it. | FIX | H | P1 | none | DOM checks.mjs PL-018: every `#hwScore .scale` has `role=group` and a label naming its element, before and after switching to F2 | dabd90b | checks.mjs PL-018 OK (four scales, role=group, labels Persona level, Task level, Context level, Format level, held after F2); DOM mode: scripts executed with no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-019 | D5 | `#s6b` · `show()` · script L2998 | `[data-fx]` buttons toggle the `act` class only (probe: `aria-pressed [null,null,null]`) | The selected starter is marked by colour alone. `show()` sets `aria-pressed` on the three buttons where it sets the class. | FIX | H | P1 | none | DOM checks.mjs PL-019: `aria-pressed` reads true,false,false at load and false,false,true after F3 | f0beec8 | checks.mjs PL-019 OK (true,false,false at load; false,false,true after F3); DOM mode: no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-020 | D7 | `#s7` · `QZ[1].f.C` L2480, `QZ[5].f.C` L2496, rendered by `fb.textContent` L2521 | Rendered: "which the evidence in &sect;03 says" and "&sect;07 covers the four checks" (probe, Chromium, item 2 and item 6 with option C) | Two feedback strings carry an HTML entity that `textContent` prints literally, the defect class JN-035 fixed in the triage. The two entities become `§`. | FIX | H | P1 | JN-035; DW-103 | DOM checks.mjs PL-020: option C on items 2 and 6 renders "§03" and "§07" and no "&sect;"; GREP "&sect;0" in the QZ block = 2 then 0 | ed9538b | checks.mjs PL-020 OK (option C on items 2 and 6 renders §03 and §07, no &sect; in the QZ block); grep -c "&sect;0" in QZ 2 then 0; DOM mode: no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-021 | D4 | `#s8` · the two h3 · L1697, L1707 | "Start a Reverse-Prompting Interview" and "Specification Diagnosis on Your Own Prompt" | Title Case on `h3`, against EDITORIAL B7 (`h2` Title Case, `h3` sentence case) and against every other `h3` on the page. "The Cole Buy-Sell Agreement" stays, a document's name and B7's own example. They become "Start a reverse-prompting interview" and "Specification diagnosis on your own prompt". | FIX | H | P1 | EDITORIAL B7; JN-024 | GREP "Reverse-Prompting Interview" = 1 then 0; DOM checks.mjs PL-021 | c21f8f1 | grep -c "Reverse-Prompting Interview" 1 then 0; checks.mjs PL-021 OK (both h3 sentence case, the document name kept); T7 unchanged 2 / 38 | LANDED |
| PL-022 | D1 | `#s8` · the paragraph under the task · L1689 | "Click each question the model asked you to reveal it. The ones you would not have thought to answer are the point." | The first sentence misparses (the model asked you to reveal it) and repeats the panel's own label, "Click each question the model actually asked you; it opens as you click", which JN-023 wrote. Authored by the pass on 09-13 (a51128e, JN-026). Reads "The questions it asked back are below. The ones you would not have thought to answer are the point." Nineteen words for twenty-one. | FIX | H | P1 | JN-026, JN-023 | GREP "asked you to reveal it" = 1 then 0; DOM checks.mjs PL-022 | 88a6e5e | grep -c "asked you to reveal it" 1 then 0; checks.mjs PL-022 OK; T7 unchanged 2 / 38 | LANDED |
| PL-023 | D1 | `#s8` · `render()` in the buy-sell block · script L3078 | The hint paragraph shown until reveal: "Ten questions, listed by number. Click the ones the model asked you and read each as it opens; the button below reveals the rest." | The third statement of the same instruction: the panel label says click and it opens, the button says "Reveal the rest", and each row says "Question n of ten". The hint line leaves the one function that renders it. | FIX | H | P1 | JN-023 | DOM checks.mjs PL-023: no `p.dim` inside `#bsell` before reveal, the label present, one click opens one question, the button opens the rest | 644e885 | checks.mjs PL-023 OK (no hint paragraph, label present, one click opens one question and counts 1 of 10, Reveal the rest opens all); DOM mode: no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-024 | D7 | `#s8` · the `p.big` · L1676 | "Wolfram makes an observation that most prompting advice skips: you can tell the model something once, as part of the prompt, and it will use it." | An attributed claim with no chip, recorded as F-088 (Low) in the 09-11 review and not fixed. A chip to `src-wolfram` needs the section name (D6, B4) and the essay is not reachable from here, so it is a source change for the instructor: name the section, chip H, add a source line. | PROPOSE | H | P2 | F-088, S2-15 | profiler `chips_total` for s8 = 0 now | (none) | reported, not landed | PROPOSED |
| PL-025 | D1 | `#s9` · the adoption chart's IIFE · script L2633-2634; the frame L1753 | Caption "THE GAP BETWEEN ROWS 1 AND 4 IS THE DELEGATION LINE" under a chart of three rows; the axis line runs to y 222 past the last row's bottom at 144 (probe) | The 41.8% row left on 09-12 (9237067) and the caption and axis still lay out for four. The caption says rows 1 and 3, the axis ends at 150, the caption moves up to 192 and the frame's `viewBox` shrinks to 206 so the chart has no empty band. One chart, one function and its own frame attribute. | FIX | H | P2 | 09-12 handback nit 1 (the axis); commit 9237067 | DOM checks.mjs PL-025: caption reads ROWS 1 AND 3, axis y2 within 8 of the last row's bottom, three bars | f6f0632 | checks.mjs PL-025 FAIL on the first draft: the caption started at x 250 and ran to about 741 in the 700 frame at HEAD as well (a JN-029-class overflow the four-row layout hid); the caption now starts at 14 like the §07 captions. Second draft OK: three bars, caption ROWS 1 AND 3, axis ends within 8 of the last row, every label inside the 700 by 206 frame. DOM mode: no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-026 | D7 | `#s10` · the case paragraph L1771 against the triage tally L2712 | "three of the six items would survive the check most people actually perform" against "Five of the six exist. Only one fails an existence check." | Two readings of the same six items on one page. The key has two sound, one misgrounded, two never-decided and one fabricated, at HEAD and in the 08-22 original alike: five survive an existence check, four survive existence plus a text check, and no reading gives three. Accuracy finding under `<classes>`: opened at the top of this turn. Correction I would make: "and five of the six would survive the check most people actually perform." A figure changes, so it is the instructor's. | PROPOSE | H | P1 | none in either audit | GREP "three of the six" = 1, "Five of the six exist" = 1; the `a:` keys: 2 sound, 1 mis, 2 nd, 1 fab | (none) | reported, opened as an accuracy flag | PROPOSED |
| PL-027 | D1 | `#s10` · Type 03 L1792 and Check 03 L1807 | "Training data has a cutoff; the Code does not." in both cards | The same sentence twice in one section, in the card that names the failure and again in the card that catches it; JN-030 then added two sentences to Check 03, which is now the longest card. It leaves Check 03, whose last sentence already gives the reason ("no source is a permanent record"), and stays in Type 03 where it first appears. Nine words fewer. | FIX | H | P1 | JN-030 | GREP "Training data has a cutoff" = 2 then 1; DOM checks.mjs PL-027 | bb08dc8 | grep -c "Training data has a cutoff" 2 then 1; checks.mjs PL-027 OK (once in §07, in Type 03, not in Check 03); T7 unchanged 2 / 38 | LANDED |
| PL-028 | D3 | `#s11` · the `p.big`, first sentence · L1826 | "Section 04, the workshop, rebuilt one of your prompts." | Inside the section whose own eyebrow reads "08 · Workshop", the apposition points at the wrong workshop; the rest of the page writes "§0N" (§01, §03, §07, §09). Entered 09-12 (9237067) when "section 6b" was retired. Reads "§04 rebuilt one of your prompts." Three words fewer; the two literal dashes later in the paragraph are untouched. | FIX | H | P1 | 09-12 commit 9237067 | GREP "Section 04, the workshop" = 1 then 0; DOM checks.mjs PL-028 | f1fbf15 | grep -c "Section 04, the workshop" 1 then 0; checks.mjs PL-028 OK (opens "§04 rebuilt one of your prompts."); T7 unchanged 2 / 38 (the two literal dashes stand) | LANDED |
| PL-029 | D1 | `#s11` · `PEER[0].d` · script L2778 | "You pasted it in and it produced something, with no changes to the template itself." | Written for the peer review, where the reader ran a partner's template; under JN-031 the instructor runs the audit and the room scores along, and 2396599 did not touch `PEER`. Reads "Pasted in as written, it produced something, with no change to the template itself." The other three criteria and the four bands already read for a live audit. | FIX | H | P1 | JN-031; D5 superseded | DOM checks.mjs PL-029: the first criterion's description text; GREP "You pasted it in" = 1 then 0 | 136062d | grep -c "You pasted it in" 1 then 0; checks.mjs PL-029 OK; DOM mode: no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-030 | D5 | `#s11` · the three `[data-tpl]` buttons L1835-1837 and their handler L2768-2773 | `act` class only (probe: `aria-pressed [null,null,null]`) | The selected template is colour-only. The three buttons gain `aria-pressed` in the markup and the handler keeps it true on the one it lights. | FIX | H | P1 | none | DOM checks.mjs PL-030: true,false,false at load; false,true,false after "Case note formatter" | 4738160 | checks.mjs PL-030 OK (true,false,false at load; false,true,false after the note template; its rubric renders); DOM mode: no thrown errors; T7 unchanged 2 / 38 | LANDED |
| PL-031 | D6 | `#s11` L1833-1841 (`t-s11`) and `#s12` L1952 (`t-s12b`), consecutive in the core order | Both are a list of items the learner ticks, with a running count and a verdict line: eight audit criteria across three templates, then seven deliverable elements | Stays. The mechanism is the same because the content is a checklist in both cases; merging would fold a template audit into a deliverable list, which are different acts, and differentiating the chrome would be cosmetic. The profiler does not flag the pair because `s12`'s first root is the capture. | LEFT-AS-IS | H | P1 | DW-094; F-7 | profiler `families` for s11 and s12 | (none) | examined, kept | LEFT-AS-IS |
| PL-032 | D2 | `#s12d` · the `p.big` · L1860 | "if a prompt has to be specified as carefully as section 04, the workshop, required to be reliable, has the tool saved time" | "required to be reliable" attaches to "the workshop" as easily as to the prompt, and "section 04, the workshop" is the form PL-028 retires. Entered 09-12 (2eb36c1). Reads "if a reliable prompt has to be specified as carefully as §04 required, has the tool saved time". Four words fewer; the question and its two halves are unchanged. | FIX | M | P2 | 09-12 commits 2eb36c1, 9237067 | GREP "section 04, the workshop" = 1 then 0; DOM checks.mjs PL-032 | c026d5f | grep -c "section 04, the workshop" 1 then 0; checks.mjs PL-032 OK; T7 unchanged 2 / 38 | LANDED |
| PL-033 | D5 | `#s12d` · `#rm1`, `#rm2` · L1904-1905 | Two text inputs with placeholders "vote 1 %" and "vote 2 %" and no label (probe: `ariaLabel null`, `labelledBy false`) | The span before them names the field but is not bound. Each input gains an `aria-label` naming the vote and the quantity. | FIX | H | P2 | none | DOM checks.mjs PL-033: both inputs carry an `aria-label` | e9bebee | checks.mjs PL-033 OK (both inputs carry an aria-label naming the vote); T7 unchanged 2 / 38 | LANDED |
| PL-034 | D7 | footer · the stamp · L2023 | "Last updated 2026-09-12." | The page changed on 09-13 and 09-14 across three merged batches and will change again in this pass; the stamp is a date, so it is the instructor's (FIX line 3). Correction I would make: the date of this pass's last commit, in the same commit. Opened at the top of this turn with PL-026. | PROPOSE | H | P1 | 09-12 handback ("the footer stamp is current"); MAINTAINING known follow-ups | GREP "Last updated 2026-09-12" = 1 | (none) | reported, opened as an accuracy flag | PROPOSED |
| PL-035 | D7 | page CSS after the fence · `.verify` L702-704, `#topbar .tb-r` L516, `#topbar kbd` L517, `.rnote` L715-716, `.pace p` L701 | Five lesson-specific rules whose selectors match nothing on the page (`class="verify` 0, `class="tb-r` 0, `<kbd` 0, `class="rnote` 0, no `p` inside `.pace`) | Dead after JN-001, JN-004, JN-018 and JN-036; JN-036 left `.verify` "for the sweep" deliberately. The managed fence's own copies are the sweep's. Page-level, so PROPOSE: delete the five rules, about ten lines, no rendered change. | PROPOSE | H | P3 | JN-036, JN-001, JN-004 | the five `grep -c` figures above; `verify-style.mjs` after (the fence is untouched) | (none) | reported, not landed | PROPOSED |
| PL-036 | D7 | script · `window.__revealBridge` L2893, `window.__revealBsell` L3089, `mark('gc')` L3222, `apply()` L3253 | Two reveal exports with no caller since JN-001; a `mark()` call on a gate JN-003 removed; two locals `shown` and `mins` computed for a readout JN-004 removed | Stays. None renders and none throws (`mark()` returns on a missing gate; DOM mode and Chromium load clean). `mark('gc')` sits inside the cold-open renderer that is byte-identical across sessions 2 to 4 (DW-066), so touching it reaches a shared block. The 09-13 handback recorded the first two as nits. | LEFT-AS-IS | H | P3 | 09-13 handback nits 2, 3; DW-066 | GREP `__revealBridge` = 1, `data-gate="gc"` = 0; DOM mode no thrown errors | (none) | examined, kept | LEFT-AS-IS |
| PL-037 | D4 | `instructor-notes/session-2.md` slots against the page eyebrows | Run-sheet slots of 9, 10, 10, 15, 10, 12, 8, 8 and 5 minutes for §01 to §09 against eyebrows of 5, 6, 6, 7, 6, 8, 5, 5 and 7 | Stays. The two figures are different quantities: the eyebrow is the design minute the 150 sums from, and the slot is the room's clock at the 09-07 audit's multiplier (D5 in the register header: "150 planned minutes, which is 255 at the 1.7x multiplier"). Every element id, `data-comp`, opener, poll and reveal the run sheet names is on the page (grep of each id, this session). | LEFT-AS-IS | H | P2 | DW-105; register header D5 | `build-appendix --check` current; each run-sheet id greps to 1 | (none) | examined, kept | LEFT-AS-IS |
